# SecondMind — Konteks Frontend

Claude Code membacanya otomatis tiap sesi.

---

## 1. Produk

SecondMind mengubah sebuah topik atau dokumen (silabus PDF) menjadi **satu tampilan hybrid roadmap + mindmap** untuk belajar. Gaya visualnya terinspirasi roadmap.sh: batang utama vertikal berisi langkah bernomor dari atas ke bawah, dengan cabang penjelas selang-seling di kiri dan kanan. Bukan dua view terpisah.

Ini final project bootcamp Hacktiv8, dikerjakan berdua.

## 2. Pembagian kerja

**Aku hanya mengerjakan frontend.** Backend (route handler, Mongoose, integrasi Gemini, next-auth, Resend, Vercel Cron) dikerjakan partnerku secara paralel dan belum selesai.

Jangan menulis route handler, skema Mongoose, atau kode integrasi Gemini. Kalau butuh data dari backend, pakai mock.

## 3. Stack

- **Framework:** Next.js (App Router) + React, PWA lewat `next-pwa` terpasang tapi **belum dikonfigurasi** (manifest/ikon/wrap `next.config.ts` — itu M10, bukan sekadar install)
- **Styling:** Tailwind CSS v4 (config CSS-first lewat `@theme` di `app/globals.css`, **bukan** `tailwind.config.*`) + `shadcn/ui` (CLI `shadcn@latest`, style `base-nova`, primitive `@base-ui/react` — bukan Radix). Token semantik shadcn (`--background`, `--primary`, `--border`, dst di `:root`) di-alias ke token `--sm-*` di §4, bukan palet abu-abu default shadcn — komponen baru ditambah lewat `npx shadcn add <nama>`, bukan ditulis manual
- **Canvas:** `@xyflow/react` (reactflow di-rename mulai v12) untuk render node, `@dagrejs/dagre` (bukan paket `dagre` lama) untuk hitung koordinat X/Y
- **State:** `zustand` (middleware `persist` untuk cache offline belum dipasang — bagian M10)
- **Auth:** `next-auth` — komponen dan session provider punya partner, aku hanya memakai `useSession`
- **Export gambar:** `html-to-image` terpasang, belum dipakai (M9)

## 4. Design token

Semua nilai ini dikunci di `app/globals.css` (`:root` + `@theme inline` — Tailwind v4 CSS-first, lihat §3). Jangan pakai warna atau font di luar daftar ini.

| Peran | Variabel | Nilai |
|---|---|---|
| Ink utama | `--sm-ink` | `#221B14` |
| Ink sekunder | `--sm-ink-70` | `#6E6255` |
| Ink tersier | `--sm-ink-45` | `#9A8B79` |
| Aksen (clay) | `--sm-clay` | `#C75B39` |
| Aksen hover/bright | `--sm-clay-bright` | `#E0794F` |
| Aksen tint (background lembut) | `--sm-clay-tint` | `#F6E7E0` |
| Paper / permukaan lembut | `--sm-paper` | `#F7F1E9` |
| Surface / permukaan utama | `--sm-surface` | `#FFFFFF` |
| Garis / border | `--sm-line` | `#E7D9C6` |

- **Font:** Plus Jakarta Sans (`--font-plus-jakarta-sans`) — satu keluarga font untuk display, body, dan metadata/angka/timeMark.

Spacing pakai skala `--sm-space-1` s/d `--sm-space-9` (4px s/d 96px, grid 4pt). Radius tiga nilai: `--sm-radius-sm` (8px), `--sm-radius-md` (12px), `--sm-radius-lg` (22px), plus `--sm-radius-pill` (999px) untuk tombol/chip. Elevasi dua nilai: `--sm-shadow-sm`, `--sm-shadow-md`.

**Token shadcn/ui:** `components/ui/*` (mis. `Button`) pakai class Tailwind semantik (`bg-primary`, `text-foreground`, `border-input`, dst), bukan `--sm-*` langsung. Supaya konsisten, `--primary` → `--sm-clay`, `--background`/`--card`/`--popover` → `--sm-surface`, `--secondary`/`--muted` → `--sm-paper`, `--accent` → `--sm-clay-tint`, `--border`/`--input` → `--sm-line`, `--ring` → `--sm-clay`, `--radius-sm/md/lg` → `--sm-radius-sm/md/lg`. Satu-satunya token shadcn yang **tidak** dipetakan ke brand: `--destructive` (masih default merah shadcn, karena belum ada warna danger di token brand). Belum ada dark mode — `.dark` sengaja tidak didefinisikan.

## 5. Kontrak data

`lib/types.ts` memisahkan dua lapis tipe:

- **Wire type** (`WireMindmap`, `WireMindmapNode`, `WireMindmapEdge`, `WireTodo`): persis kontrak backend yang sudah disepakati dengan partner. Jangan tambah field di sini kecuali partner konfirmasi field itu memang dikirim backend.
- **UI type** (`Mindmap`, `MindmapNode`, `MindmapEdge`): turunan buat reactflow. Boleh punya field tambahan yang **dihitung di frontend** — bukan field baru dari backend.

```ts
// lib/types.ts
import type { Edge, Node } from "@xyflow/react";

export type WireNodeType = "roadmap-step" | "mindmap-branch";

export type WireMindmapNode = {
  id: string;
  type: WireNodeType;
  data: {
    label: string;
    description: string;
    timeOffsetDays: number | null; // integer mentah dari Gemini, frontend format jadi "Hari X"/"Minggu Y"
  };
};

export type WireMindmapEdge = {
  id: string;
  source: string;
  target: string;
};

export type WireMindmap = {
  _id: string;
  title: string;
  topic: string;
  timeframe: string;
  language: string;
  feasibilityWarning: string | null;
  isPublic: boolean;
  shareId: string | null;
  startDate: string;
  nodes: WireMindmapNode[];
  edges: WireMindmapEdge[];
};

export type WireTodo = {
  _id: string;
  mindmapId: string;
  taskText: string;
  dueDate: string;
  isCompleted: boolean;
};

// UI types
export type MindmapNodeData = WireMindmapNode["data"] & {
  num?: string; // nomor urut roadmap-step, dihitung dari index — bukan dari backend
  isActive?: boolean; // state highlight lokal (mis. cabang lagi di-elaborate) — bukan dari backend
  isCompleted?: boolean; // toggle "tandai selesai" lokal, cuma dipakai roadmap-step — bukan dari backend
  timeMark?: string | null; // "Hari X"/"Minggu Y", diformat dari timeOffsetDays — bukan dari backend
};

export type MindmapNode = Node<MindmapNodeData, WireNodeType>; // Node<> mewajibkan `position`
export type MindmapEdge = Edge;

export type Mindmap = Omit<WireMindmap, "nodes" | "edges"> & {
  nodes: MindmapNode[];
  edges: MindmapEdge[];
};
```

**Penting:** node dari backend **tidak** punya field `position`. Koordinat dihitung di frontend pakai dagre setiap kali graph di-load atau bertambah, lewat `lib/canvas/layout.ts`. Kalau backend mengirim `position`, abaikan.

**Migrasi `timeMark` → `timeOffsetDays` sudah kelar** (matching `database_schema.md`, ditulis partner). Wire type sekarang `timeOffsetDays: number | null`; `lib/api.ts`'s `toMindmap()` yang format jadi tampilan "Hari X"/"Minggu Y" (disimpan balik ke `timeMark` — tapi sekarang field UI-only hasil hitungan, bukan lagi field wire). Komponen (`RoadmapStepNode.tsx`, `MindmapBranchNode.tsx`, `Drawer.tsx`) gak berubah sama sekali karena masih baca `data.timeMark` yang sama, cuma sumbernya sekarang dihitung bukan dikirim mentah. *(Catatan lama yang masih relevan: `api_documentation.md` contoh response `generate` masih nunjukin `timeMark: "Day 1"`, gak sinkron sama `database_schema.md` — kemungkinan dokumen itu belum di-update, worth dikonfirmasi ke partner.)*

**Konversi wire→UI** ditaruh di dua tempat, bukan di komponen:
- `lib/api.ts` (`toMindmap()`): memetakan `WireMindmap` → `Mindmap`, mengisi `num` (index roadmap-step + 1), `timeMark` (format tampilan dari `timeOffsetDays`), dan `position` placeholder awal.
- `lib/canvas/layout.ts`: mengisi `position` final dari hasil dagre.

## 6. Endpoint backend

Semua dipanggil hanya lewat `lib/api.ts`, tidak pernah langsung dari komponen.

| Endpoint | Kegunaan |
|---|---|
| `POST /api/mindmap/generate` | Kirim `FormData` (topik, PDF, timeframe, verbosity, language). Bisa makan waktu sampai 60 detik. Backend langsung auto-save ke MongoDB — response-nya sudah dokumen lengkap dengan `_id`, bukan preview yang perlu di-save terpisah. |
| `POST /api/mindmap/elaborate` | JSON `{ nodeId, concept, action, language }` → balik `{ newNodes, newEdges }` |
| `GET /api/mindmap` | Daftar ringkas untuk dashboard — cuma `_id, title, topic, timeframe, createdAt, isPublic, shareId` (bukan `nodes`/`edges` penuh, hemat bandwidth) |
| `GET /api/mindmap/:id` | Mindmap lengkap |
| `PUT /api/mindmap/:id` | Simpan `{ nodes, edges }` hasil edit manual. Juga nerima `isPublic: boolean` — kalau `true` dan mindmap belum punya `shareId`, backend auto-generate `shareId` baru sendiri (jangan bikin di frontend). |
| `DELETE /api/mindmap/:id` | Hapus. Backend cascade-delete semua `Todo` terkait juga, biar cron gak ngirim reminder buat mindmap yang udah gak ada. |
| `GET /api/mindmap/share/:shareId` | Publik, tanpa session — untuk halaman read-only. `403` kalau mindmap-nya gak `isPublic`. |
| `POST /api/todo`, `GET /api/todo?mindmapId=`, `PUT /api/todo/:id` | CRUD to-do |

Error yang perlu ditangani di UI: `400` (format file salah), `413` (PDF terlalu besar), `401` (session habis/belum login — semua endpoint `/mindmap/*` dan `/todo/*` butuh session), `403` (share link private), `404` (mindmap/todo tidak ditemukan).

## 7. Aturan arsitektur

1. **Semua akses data lewat `lib/api.ts`.** Selama backend belum siap, isinya mengembalikan mock dari `lib/mock/`. Integrasi nanti cukup mengganti isi file ini, bukan menyisir komponen. *(Berlaku untuk `nodes`/`edges` — `store/canvasStore.ts` sekarang load lewat `getMindmap()`. Metadata mindmap (`title`/`topic`/`timeframe`/`createdAt`) gak disimpan di store — tetap gak ada rencana nambah field ke store buat ini, karena udah terbukti cukup diteruskan sebagai props biasa dari `page.tsx` yang nyimpen `mindmap` di state lokal, turun ke `Drawer`/`ExportButton`/`ShareButton` yang butuh. `Drawer.tsx` sempat jadi satu-satunya sisa yang masih import `MOCK_MINDMAP` langsung buat `topic` — udah dibenerin, sekarang nerima `mindmapTopic` sebagai prop kayak yang lain.)*
2. **Logika dagre diisolasi di `lib/canvas/layout.ts`**, tidak boleh ada di dalam komponen.
3. **Store zustand dikonsumsi dengan selector spesifik** (`useStore(s => s.nodes)`), bukan mengambil seluruh store. Kalau tidak, canvas re-render tiap klik satu node.
4. **Drag node dimatikan.** Pan, zoom, dan fit view tetap aktif.
5. **Status online/offline dipegang satu listener di root** dan disimpan di store. Jangan menaruh `navigator.onLine` di tiap komponen.

Bentuk store yang disepakati — state: `nodes`, `edges`, `selectedNodeId`, `status`, `isOnline`. Actions: `setGraph`, `selectNode`, `clearSelection`, `toggleNodeComplete`, `appendNodes`, `applyLayout`. *(Store nyata sekarang punya `nodes, edges, selectedNodeId` + `onNodesChange, onEdgesChange, selectNode, clearSelection, toggleNodeComplete, setGraph`. `setGraph(nodes, edges)` motong lewat `getLayoutedElements` sekalian — dipanggil sekali pas modul store di-load, dari `getMindmap()` di `lib/api.ts`. Masih kurang `status`, `isOnline`, `appendNodes`, `applyLayout` — ditunda ke M7/M10 karena baru kepake pas ada alur elaborate/offline beneran.)*

## 8. Cara kerja yang aku harapkan

- Kerjakan **satu milestone per sesi**, lalu berhenti dan laporkan sebelum lanjut.
- Kalau ada keputusan yang mahal untuk dibalik (bentuk store, struktur folder komponen, bentuk props node, token desain/identitas visual), tanya dulu sebelum menulis kode.
- Untuk hal kecil, langsung kerjakan saja tanpa bertanya.
- Jelaskan singkat praktik yang kamu pakai kalau itu konvensi tim profesional yang mungkin belum aku tahu.
- Jangan menulis kode backend.

---

## 9. Backlog

Status per 2026-08-17, dicek ulang terhadap working tree — bukan cuma niat.

### M0 — Setup
- [x] Init Next.js App Router + Tailwind
- [x] Install shadcn/ui, reactflow, dagre, zustand, next-pwa, html-to-image — semua terpasang. `shadcn/ui` sudah di-`init` dan token semantiknya dipetakan ke `--sm-*` (lihat §4); `next-pwa` baru package-nya, konfigurasi manifest/service-worker masih M10
- [x] Struktur folder: `components/`, `store/`, `lib/`, `lib/mock/`
- [x] Tulis `lib/types.ts` sesuai bagian 5 (wire/UI split)
- [x] Masukkan design token bagian 4 ke `app/globals.css` — sudah ada lewat `@theme inline`, §4 sudah dikoreksi supaya dokumen ikut kode

### M1 — Kerangka halaman
- [x] Header: tombol kembali ✓, judul ✓, tombol aksi ✓, progress bar ✓ (`header-progress` di `app/canvas/[id]/page.tsx`, dihitung dari node `roadmap-step`)
- [x] Canvas reactflow kosong — pan/zoom/fit view aktif ✓, `nodesDraggable={false}` sudah di-set di `CanvasView.tsx`
- [x] Sidebar kanan (bisa buka-tutup) — `components/canvas/Drawer.tsx`
- [x] Input chat di bawah canvas — ada di `CanvasView.tsx`, belum wired ke aksi (wajar, itu M7)

**Gotcha React Flow + data async** (kena pas M11 wiring `/canvas/[id]`): `<ReactFlow fitView>` cuma nge-fit sekali pas mount. Kalau `<CanvasView />` di-mount duluan sebelum `getMindmap()` selesai (nodes masih `[]`), canvas kejebak di state kosong/rusak biarpun data numpang masuk belakangan — `fitView` gak pernah re-run. Fix-nya: `app/canvas/[id]/page.tsx` sekarang nahan render `<CanvasView />` sampai `mindmap` state kepenuhin (`{mindmap ? <CanvasView /> : <div className="canvas-loading">...</div>}`). **Berlaku juga nanti buat M7**: begitu `appendNodes`/`applyLayout` dibikin, node baru yang nambah ke canvas yang udah ke-mount duluan gak akan otomatis ke-fit — butuh `useReactFlow().fitView()` manual, bukan cuma andelin prop `fitView`.

### M2 — Lapisan data
- [x] Mock mindmap di `lib/mock/mindmap.ts` — bentuk `WireMindmap` murni (bukan `Mindmap`), 5 roadmap-step + 13 branch, `timeOffsetDays` terisi (7/14/21/28/35), tanpa `position`/`num`/`isActive`
- [x] Mock todo di `lib/mock/todo.ts` — 3 `WireTodo` seed, terkait ke mindmap mock
- [x] `lib/api.ts`: `generateMindmap`, `elaborateNode`, `getMindmaps`, `getMindmap`, `saveMindmap`, `deleteMindmap`, `getTodos`, `createTodo`, `updateTodo` — semua ada, return mock lewat "database" in-memory + delay 300ms simulasi network. `getMindmaps` dipakai composer, `saveMindmap` belum dipanggil komponen manapun (wajar, itu M11)
- [x] Komponen tidak boleh fetch langsung — `store/canvasStore.ts` panggil `getMindmap()` dari `lib/api.ts`, bukan import `MOCK_MINDMAP` langsung (lihat §7 poin 1 buat gap yang tersisa: metadata mindmap di halaman lain)
- [x] Migrasi `timeMark: string` → `timeOffsetDays: number` di `lib/types.ts` + format "Hari X"/"Minggu Y" pas mapping wire→UI di `lib/api.ts` (lihat §5) — kelar, `lib/mock/mindmap.ts` dan komponen node/drawer udah konsisten

### M3 — Store zustand
- [~] State dan actions sesuai bagian 7 — `clearSelection`, `toggleNodeComplete`, `setGraph`, `appendNodes`, `applyLayout` (lihat M7) sudah ada; `status`, `isOnline` masih ditunda ke M10 (offline-related, belum kepake sampai fitur itu jalan)
- [x] Konsumsi pakai selector spesifik — semua pemakaian sudah `useCanvasStore(s => s.x)`

### M4 — Node dan layout
- [x] Komponen `roadmap-step`: bernomor ✓, lebih besar ✓, tampil `timeMark` ✓, warna berubah + ikon centang kalau selesai ✓ (`.xf-step.is-complete`)
- [x] Komponen `mindmap-branch`: lebih kecil, tanpa nomor
- [x] `lib/canvas/layout.ts`: spine vertikal (dagre TB, step-only) + cabang tiap step di-fan manual ke kiri/kanan gantian per step, sesuai hero illustration di `app/page.tsx`
- [x] Daftarkan keduanya ke `nodeTypes`

**⚠️ "Tandai selesai" (`isCompleted` di node) gak ada di spec resmi.** Dicek ulang ke `user_flow.md` dan `database_schema.md` (root, punya partner): skema node Mindmap cuma punya `label`, `description`, `timeOffsetDays` — gak ada field completion. `isCompleted` di spec cuma ada di skema `Todo` (M8). Konsekuensinya: state "selesai" di node sekarang **cuma di zustand lokal**, bakal ke-drop diam-diam kalau `PUT /api/mindmap/:id` beneran dipanggil (backend gak punya kolom buat nyimpennya). Belum diputuskan: biarin sebagai visual lokal-saja, atau ganti jadi ngikutin status To-Do (M8) yang emang persisted.
  - **Re-dicek 2026-08-18 (user nanya kenapa ilang pas refresh)**: baca langsung `models/Mindmap.ts` yang sekarang — konfirmasi ulang, masih beneran gak ada kolom completion di `nodes[].data` (cuma `label`, `description`, `timeOffsetDays`, plus satu field baru `userNotes` yang gak ada sebelumnya, lihat catatan di bawah). Jadi bukan bug frontend yang kelewat nyimpen — frontend gak punya tempat buat nyimpennya, `PUT` bakal nge-drop field itu diam-diam (Mongoose strict mode) walau dipaksa dikirim. Fix beneran butuh backend nambah kolom, di luar scope kerjaan frontend (§2) — ditanyakan balik ke user gimana mau lanjut (tunggu partner nambah kolom, atau geser semantiknya ke status To-Do yang udah persisted).
  - **Temuan sampingan pas re-cek skema**: `models/Mindmap.ts` sekarang punya field baru `data.userNotes: string` (default `""`) yang **belum ada** di `WireMindmapNode` (`lib/types.ts`) atau didokumentasikan di §5 — kemungkinan besar ini yang dimaksud buat isi textarea "Catatan" di `Drawer.tsx` (sekarang `<textarea>` polos tanpa state/onChange sama sekali, murni UI kosong). Belum dikerjakan, di luar task yang diminta sesi ini — dicatat buat sesi berikutnya.
  - **✅ Resolved 2026-08-18**: Zufar nambahin `isCompleted: { type: Boolean, default: false }` ke `nodes[].data` di `models/Mindmap.ts`, persis sesuai pesan yang dikirim. Frontend langsung di-wire: `WireMindmapNode["data"]` (`lib/types.ts`) sekarang punya `isCompleted: boolean` sebagai field wire asli (bukan lagi field UI-only) — `MindmapNodeData`'s tambahan lokal (`num`, `isActive`, `timeMark`) gak lagi include `isCompleted` karena udah diwarisi dari wire type. `saveMindmap()` (`lib/api.ts`) yang tadinya whitelist field `nodes[].data` cuma `label`/`description`/`timeOffsetDays` (jadi diam-diam buang `isCompleted` walau dikirim) ditambah `isCompleted` ke whitelist-nya. `Drawer.tsx` dapat `handleToggleComplete()` baru — pola optimistic-update-lalu-revert yang sama kayak rename (bukan pola save-first kayak delete, karena toggle status selesai itu reversible/gak destruktif): `toggleNodeComplete()` store action jalan dulu, lalu `saveMindmap()`; gagal → toggle balik ke state semula + `.field-error` "Gagal menyimpan status selesai.". Tombol dapat state `isTogglingComplete` (disabled selagi nyimpen) biar gak race kalau diklik cepat berturut-turut. `elaborateNode()` di `lib/api.ts` juga di-update — cabang baru hasil expand eksplisit di-set `isCompleted: false` (Gemini gak pernah balikin field ini buat node baru). Diverifikasi lewat Playwright: toggle selesai → `PUT /api/mindmap/:id` beneran kekirim dengan `isCompleted:true` di body → **reload** → status "Sudah selesai" tetap ada (beneran ke MongoDB, bukan lokal doang) → toggle balik jadi belum-selesai → reload lagi → tetap konsisten kedua arah.

**`userNotes` (textarea "Catatan" di Drawer) — sekarang beneran nyimpen (user lapor "belum bisa simpan").** Field `data.userNotes: string` udah kebawa dari merge `development`→`frontend` (Zufar udah nambahin state `notesDraft` + wire type-nya), tapi `updateNodeNotes` (store action) gak pernah dipanggil dari mana pun — ketikan di textarea cuma react state lokal yang ilang gitu aja, gak pernah nyampe backend. Fix: `handleSaveNotes()` baru di `Drawer.tsx`, dipicu `onBlur` textarea (bukan tombol simpan eksplisit kayak rename — notes sifatnya freeform/low-stakes, auto-save-on-blur pola standar buat field kayak gini) — kalau `notesDraft` beda dari `node.data.userNotes` sebelumnya, panggil `updateNodeNotes(node.id, notesDraft)` (optimistic) lalu `saveMindmap()`; gagal → revert ke notes lama + `.field-error` "Gagal menyimpan catatan.". State `isSavingNotes` (udah ada duluan dari Zufar, cuma belum kepake) sekarang beneran dipakai buat disable textarea selagi nyimpen.
  - **Bug lebih dalam ketemu pas investigasi**: `PUT` request-nya beneran kekirim dengan teks catatan yang bener DAN backend beneran nyimpen (dikonfirmasi lewat raw `GET` langsung ke API, bypass semua state React) — tapi abis reload, textarea tetap kosong. Root cause-nya BUKAN di save-nya, tapi di cara `Drawer.tsx` nge-restore state pas node pertama kali diklik: `const [prevNodeId, setPrevNodeId] = useState(selectedNodeId)` — pola "adjust state during render" yang dipakai buat reset `notesDraft`/dll pas ganti node cuma jalan kalau `selectedNodeId !== prevNodeId`. Masalahnya, `Drawer` cuma ke-mount SETELAH ada node yang keklik (`isSidebarOpen` di `page.tsx`), jadi pas Drawer pertama kali mount, `selectedNodeId` UDAH keisi node yang diklik itu — `prevNodeId` yang di-seed dari `selectedNodeId` otomatis jadi SAMA PERSIS, kondisi resetnya gak pernah `true` buat node pertama yang diklik, dan `setNotesDraft(node?.data.userNotes ?? "")` gak pernah kepanggil sama sekali. Fix: `prevNodeId` di-seed dari `null` (bukan `selectedNodeId`), bukan sentinel yang bisa ketemu beneran, jadi node pertama yang diklik SELALU beda dari `prevNodeId` awal dan block reset-nya jalan. Bug ini juga kena tiap kali klik node pertama kalinya secara umum (bukan cuma abis reload) — cuma baru ketauan sekarang karena sebelumnya `notesDraft` gak pernah dipakai buat apa-apa yang keliatan.
  - Diverifikasi lewat Playwright: isi catatan → klik area lain (blur) → `PUT` kekirim dengan teks yang bener → **reload** → catatan tetap ada di textarea → kosongin catatan → blur → reload lagi → tetap kosong (round-trip string kosong juga bener, bukan cuma nulis doang).

### M5 — Interaksi
- [x] Klik node → sidebar tampilkan label, description, timeMark
- [x] **Bug ketemu user**: indikator node yang lagi aktif/dipilih di canvas kurang jelas. `RoadmapStepNode.tsx`/`MindmapBranchNode.tsx` sebenarnya udah baca `selected` dari React Flow (`NodeProps.selected` — otomatis ke-set lewat `applyNodeChanges` pas node di-klik, terpisah dari `selectedNodeId` di canvasStore) dan nge-toggle class `is-selected`, tapi stylingnya (`canvas.css`) kurang nonjol: step cuma dikasih `box-shadow: 0 0 0 2px var(--sm-clay)` — ring tipis warna clay yang HAMPIR GAK KELIHATAN kalau node-nya lagi `.is-complete` juga (background-nya udah clay, ring clay nyaris nyatu sama background). Fix: `.xf-step.is-selected` sekarang pakai double-ring (`0 0 0 3px var(--sm-paper), 0 0 0 6px var(--sm-clay-bright)` — cincin krem dulu baru cincin oranye terang di luarnya, biar kontras kebaca di background apa pun termasuk `.is-complete`) plus `transform: translateY(-2px)` biar node-nya keliatan "terangkat". `.xf-branch.is-active`/`.is-selected` ditambah `box-shadow: 0 0 0 4px var(--sm-clay-tint)` (halo lembut) dan `translateY(-1px)` senada, di atas border 2px clay yang udah ada. Diverifikasi lewat Playwright (`getComputedStyle` + screenshot): ring dan transform kebaca jelas dan sesuai nilai yang ditulis di CSS, kelihatan jauh lebih menonjol dibanding sebelumnya di kedua tipe node.
  - **Susulan (user minta)**: klik cabang (mindmap-branch) sekarang bikin langkah induknya (roadmap-step) ikut nyala juga di canvas, bukan cuma kebaca di drawer. Dipakein field `isActive` yang ternyata udah lama ada di `MindmapNodeData` (`lib/types.ts`) tapi gak pernah ke-set siapa pun (dead field, komentar lama bilang buat "cabang lagi di-elaborate" tapi gak pernah diimplementasi). `CanvasView.tsx` sekarang ngitung `activeParentStepId` (cari edge yang `target`-nya = `selectedNodeId`, ambil `source`-nya, cuma kalau node yang dipilih itu `mindmap-branch`) tiap render, terus bikin `displayNodes` (turunan dari `nodes` store, bukan nulis balik ke store — murni transformasi tampilan) yang nyuntik `data.isActive = true` ke step yang cocok sebelum dioper ke `<ReactFlow nodes={displayNodes}>`. `RoadmapStepNode.tsx` baca `data.isActive` buat toggle class `is-active`. Diverifikasi lewat Playwright: klik cabang → persis 1 step dapat class `is-active`, dan step itu beneran match sama nama parent step yang ditampilkan drawer (`.parent-step-label`) → klik step langsung (bukan cabang) → gak ada step yang `is-active` (logic-nya emang cuma buat highlight induk dari cabang, bukan buat step yang diklik langsung) → klik pane kosong (deselect) → `is-active` ke-clear juga.
  - **Fix susulan (user kirim screenshot, "gak nyala")**: iterasi pertama sengaja bikin `.xf-step.is-active` beda dari `.is-selected` (single ring `--sm-clay-tint` tanpa lift) biar gak ambigu "step mana yang beneran diklik". Ternyata dua masalah: (1) user maunya step induk nyala **sama kuat** kayak abis diklik langsung, bukan versi lebih lembut; (2) `--sm-clay-tint` (#F6E7E0) warnanya nyaris sama kayak `--sm-paper` (#F7F1E9, background canvas) — ring-nya nyaris gak kebaca sama sekali di screenshot user. Fix: `.xf-step.is-active` disatuin ke selector yang sama persis kayak `.xf-step.is-selected` (double-ring `--sm-paper`+`--sm-clay-bright` plus `translateY(-2px)`) — sekarang step induk nyala identik sama step yang diklik langsung. Diverifikasi ulang lewat Playwright + screenshot: ring dobel jelas kebaca, gak ketuker sama warna background.
  - **Fix susulan lagi (setelah merge `development`→`frontend` bawa layout multi-level dari Zufar — §11 M11-adjacent)**: dua bug ketemu user pas nyoba di layout baru yang cabangnya bisa nested berlapis (mindmap-branch punya sub-mindmap-branch, bukan cuma satu level dari roadmap-step):
    1. **"Cuma dua node nyala"** — `activeParentStepId` versi lama cuma jalan SATU hop ke atas (`edges.find(e => e.target === selectedNode.id)?.source`), jadi kalau cabang yang diklik nested (level-2+), yang ke-set `isActive` malah cabang INDUKNYA LANGSUNG (yang juga tipe `mindmap-branch`, bukan `roadmap-step`) — dan karena `MindmapBranchNode.tsx` JUGA baca `data.isActive` buat class `.is-active`-nya sendiri, cabang perantara itu ikut kelihatan "nyala" kayak node yang di-highlight, padahal roadmap-step aslinya (akar dari seluruh rantai) gak kesentuh sama sekali. Fix: `CanvasView.tsx` sekarang jalan ke atas **looping** (bukan cuma sekali) lewat rantai edge sampai ketemu node bertipe `roadmap-step` beneran (ada guard `visited` set biar gak infinite-loop kalau ada siklus data yang aneh), baru itu yang di-set `isActive`. Efek sampingnya otomatis benerin juga: karena `activeParentStepId` sekarang gak pernah lagi berupa id cabang, `MindmapBranchNode` gak akan pernah ke-mark `isActive` lagi — cuma `roadmap-step` asli yang bisa nyala.
    2. **"Tombol close drawer harusnya bikin gak ada node aktif"** — root cause-nya beda dari yang di atas: `.is-selected` (buat node yang LAGI diklik, bukan induknya) ternyata masih baca prop `selected` bawaan React Flow (`NodeProps.selected`), yang statusnya di-track INTERNAL sama React Flow sendiri lewat `onNodesChange`/`applyNodeChanges` — terpisah total dari `selectedNodeId` di `canvasStore`. Tombol close drawer manggil `clearSelection()` yang cuma nge-reset `selectedNodeId` di canvasStore doang — gak pernah ngirim event ke React Flow buat bilang "gak ada yang selected lagi", jadi ring `.is-selected` di node yang terakhir diklik nyangkut nempel walau drawer-nya udah ketutup. Fix: berhenti sepenuhnya gantungin ke `selected` dari React Flow — `CanvasView.tsx` sekarang ngitung sendiri `data.isSelected = n.id === selectedNodeId` di `displayNodes` (field baru di `MindmapNodeData`, `lib/types.ts`), dan `RoadmapStepNode.tsx`/`MindmapBranchNode.tsx` baca `data.isSelected` itu, bukan lagi destructure `selected` dari `NodeProps` sama sekali. Karena `selectedNodeId` satu-satunya sumber kebenaran sekarang murni dari canvasStore, `clearSelection()` dari mana pun (tombol close drawer, klik pane kosong, dll) otomatis bikin `isSelected` ke-clear konsisten di semua node — gak ada lagi state ganda yang bisa out-of-sync.
    - Diverifikasi lewat Playwright (skenario nested asli): klik cabang level-1 → "Pecah jadi sub-cabang" (bikin cabang level-2 di bawahnya) → klik salah satu cabang level-2 → persis 1 `roadmap-step` (akarnya, dikonfirmasi cocok sama `.parent-step-label` di drawer) dapat `.is-active`, nol cabang perantara ikut nyala → klik tombol close drawer → nol node sisa yang punya `.is-selected` ATAU `.is-active`.
  - **Susulan lagi (user minta "satu jalur menyala semua")**: sebelumnya cuma roadmap-step akar yang nyala, cabang-cabang perantara di antaranya gak ikut — user minta SELURUH jalur (tiap cabang perantara + step akar) nyala bareng, biar kebaca kayak jejak dari node yang diklik sampai ke step paling atas. Fix: `activeParentStepId` (single id) diganti jadi `activePathIds` (`Set<string>`) — loop jalan-ke-atas yang sama sekarang nyimpen SEMUA node yang dilewatin, bukan cuma yang terakhir. **Bug ketemu pas nulis ulang loopnya**: awalnya guard anti-infinite-loop (`visited`) dan Set hasil (`activePathIds`) dijadiin SATU Set yang sama — begitu satu node ditambahin ke situ di iterasi ini, iterasi berikutnya ngecek node itu lagi buat lanjut naik dan nemuin dia "udah ada di Set", jadi loop berhenti SATU LANGKAH LEBIH AWAL (cabang perantara nyala tapi roadmap-step akarnya gak kesentuh — reproduksinya jelas: `active step count=0, active branch count=1` pas ditest). Fix beneran: pisah jadi dua Set independen — `visited` (khusus cycle-guard, gak pernah dibaca buat hasil) dan `activePathIds` (khusus hasil, dikembaliin ke `displayNodes`). `MindmapBranchNode`/`RoadmapStepNode` gak perlu diubah sama sekali — dua-duanya udah baca `data.isActive`, tinggal Set-nya yang sekarang bisa punya lebih dari 1 anggota. Diverifikasi lewat Playwright (skenario nested yang sama) + screenshot: klik cabang level-2 → cabang level-2 (selected, ring), cabang level-1 (is-active, border+halo), DAN roadmap-step akar (is-active, ring dobel) tiga-tiganya nyala bareng; step-step lain di spine tetap gak kesentuh (cuma 1 step yang `is-active`); tombol close drawer tetap bersihin semuanya.
- [x] Tombol tandai selesai — `Drawer.tsx`, cuma tampil untuk `roadmap-step`, manggil `toggleNodeComplete` (lihat catatan di M4 soal persistensi)
- [x] Progress bar dihitung dari node `roadmap-step` saja — header `app/canvas/page.tsx`
- [x] Tutup sidebar → seleksi hilang (`clearSelection`)
- **Manual Node Edits (Rename, Delete, Drag Wires)** — item dari `user_flow.md` (`I --> J[Manual Node Edits] --> |Rename, Delete, Drag Wires| I`) yang kelewat gak ke-breakdown ke milestone mana pun sejak awal project, ketemu pas sinkronisasi ulang ke `user_flow.md` (lihat §11). Backend-nya udah siap dari awal — `PUT /api/mindmap/:id` (§6) emang didesain buat "Simpan `{ nodes, edges }` hasil edit manual", dan `saveMindmap()` di `lib/api.ts` udah manggil endpoint itu beneran (bukan mock, sejak M9):
  - [x] **Rename** — `store/canvasStore.ts` dapat action baru `renameNode(id, label)`. UI-nya **bukan** langsung-bisa-diketik (iterasi pertama kayak gitu, ditolak user: "jangan langsung edit seperti itu, harus ada tombol edit... tombol untuk savenya juga") — sekarang judul default tampil sebagai teks statis (`.node-title-static`, `<strong>`) plus ikon pensil "Edit" di `node-head-row` baru. Klik Edit → ganti jadi `<input className="node-title">` terkontrol (`titleDraft` state) plus ikon centang "Simpan" dan silang "Batal" (gantiin ikon pensil/hapus selagi mode edit). Simpan (via tombol, atau Enter) manggil `renameNode()` lalu `saveMindmap(mindmapId, { nodes: updatedNodes })`; gagal → revert `renameNode` ke label lama + tampilin `.field-error`. Batal (tombol, atau Escape) buang draft tanpa nyimpen apa pun. Ganti node yang dipilih otomatis keluar dari mode edit (state `prevNodeId`, pola "adjust state saat render" yang sama kayak `sidebarTab` di M7, ngindarin lint `react-hooks/set-state-in-effect`). Diverifikasi lewat Playwright: default statis (bukan input) ✓, klik Edit → Simpan/Batal muncul ✓, Batal gak ubah apa pun ✓, Simpan → keliatan di canvas → **reload** → tetap ada (beneran ke-`PUT` MongoDB) ✓.
  - [x] **Delete** — ikon tempat sampah di sebelah ikon Edit (`node-head-actions`), buka `Dialog` konfirmasi (pola sama persis kayak delete-mindmap di composer: ikon lingkaran destructive, judul "Hapus node ini?" + label node di baris terpisah dengan tanda kutip, deskripsi beda buat step vs branch, tombol Batal/Hapus). Logika hapusnya **bukan** sekadar buang satu node — fungsi baru `removeNodeCascade(nodes, edges, idToDelete)` di `lib/canvas/layout.ts`: kalau yang dihapus `mindmap-branch`, cuma node+edge itu doang yang ilang; kalau `roadmap-step`, cabang-cabangnya ikut kehapus (biar gak nyisain node nyantol tanpa induk — bakal jatuh ke posisi fallback {x:0,y:0} kalau dibiarin), spine-nya disambung ulang (edge baru step-sebelum → step-sesudah, biar rantai roadmap gak putus), dan nomor urut (`num`) semua step yang tersisa di-hitung ulang dari 01 (gak ada lompatan nomor). Alur konfirmasi: `saveMindmap()` dipanggil DULU dengan hasil "preview" cascade (`removeNodeCascade` dipanggil di komponen, bukan cuma di store) — kalau sukses baru store-nya (`deleteNode(id)`, manggil `removeNodeCascade` lagi secara internal) beneran di-update + dialog ketutup + drawer ketutup (`onClose()`); kalau gagal, dialog tetap kebuka dengan `deleteError`, store gak disentuh sama sekali. Ini sengaja beda pola dari rename (yang optimistic-update-lalu-revert) — delete itu destruktif, jadi urutannya "pastiin backend oke dulu baru ubah UI", bukan "ubah UI dulu baru revert kalau gagal". Diverifikasi lewat Playwright dua skenario: hapus `mindmap-branch` (cuma -1 node) dan hapus `roadmap-step` yang punya 2 cabang di tengah rantai (total -3 node, spine tersambung ulang, penomoran 01-05 tanpa lompat, posisi node lain gak berantakan — dicek lewat dump `transform` tiap node sebelum/sesudah) — keduanya bener, plus persist setelah reload.
  - [x] **Drag Wires** — dikerjain Zufar di `frontend-zufar` (bukan aku), masuk lewat merge `development`→`frontend` 2026-08-18. `nodesConnectable={true}`, `edgesReconnectable={true}` di `CanvasView.tsx`, handler `onConnect`/`onReconnect`/`onEdgeDoubleClick` (double-klik edge buat hapus)/`onEdgesDelete`, semua manggil `saveMindmap()` abis mutasi. Store dapet action baru `connectEdge`/`reconnectEdge`/`deleteEdge`. Node component (`RoadmapStepNode.tsx`/`MindmapBranchNode.tsx`) diganti pakai `BiHandle` (dua `<Handle>` per sisi — `target` dan `source` masing-masing dengan id `-target`/`-source` suffix) biar bisa mulai/berhenti drag wire dari sisi mana pun. **Bug asli ketemu pas resolve conflict** (bukan dari konfliknya sendiri): `BiHandle` awalnya baca `connectionNodeId` dari `useStore()` React Flow buat tau lagi ada drag-in-progress atau nggak — property itu udah gak ada di versi `@xyflow/react` yang kepasang (`tsc` langsung nunjukin error). Fix: ganti ke `useConnection((c) => c.inProgress)`, API resmi buat itu di versi sekarang.
  - **Temuan sampingan pas nulis test delete** (belum di-fix, di luar scope tugas ini): "Cabang di langkah ini" (`siblings` di `Drawer.tsx`) buat node `roadmap-step` filternya cuma `edges.filter(e => e.source === stepId)` tanpa cek `type` — ini juga nangkep edge spine ke step SELANJUTNYA (bukan cuma edge ke branch), jadi kemungkinan step berikutnya kadang nongol keliru di seksi "cabang". Gak kena bug pas verifikasi delete (`removeNodeCascade`-nya sendiri udah benar filter by `type === "mindmap-branch"`), tapi UI listnya berpotensi nampilin data yang salah.

### M6 — Alur generate
- [x] Form: topik ✓ (controlled + validasi wajib isi), upload PDF ✓ (`File` asli ke-simpen), timeframe/verbosity/bahasa ✓ (dibaca lewat ref pas submit). `handleGenerateClick` di `app/composer/page.tsx` ngerakit `GenerateMindmapInput`, taruh ke `lib/pendingGenerate.ts` (module singleton `setPendingGenerateInput`/`takePendingGenerateInput` — bukan `sessionStorage`, karena `input.file` adalah objek `File` asli yang gak bisa diserialisasi ke string; client-side nav Next.js gak reload JS runtime jadi module state cukup), lalu `router.push("/loading")`.
- [x] Loading state — `app/loading/page.tsx` sekarang manggil `generateMindmap()` sungguhan (dari `lib/pendingGenerate`'s payload) sambil teks progres bertahap tetap jalan; sukses → `router.replace(\`/canvas/${mindmap._id}\`)` (bukan `/canvas` statis lagi). Teks progres (6 tahap, `MESSAGES` di `app/loading/page.tsx`) berhenti di pesan terakhir alih-alih muter balik ke awal (`Math.min`, bukan `% length`) — biar gak kelihatan ngulang pas prosesnya lama. Animasi di bawah teks: progress bar (`.build-progress`/`.build-progress-fill`) dengan fill lebar dinamis dari `messageIndex` dikapsulasi max 90% (baru ke 100% pas sukses) supaya gak kesan "selesai" padahal masih proses, plus shimmer halus di dalam fill-nya.
- [x] Error state + tombol coba lagi — kalau `generateMindmap()` reject, tampil pesan gagal + tombol "Coba Lagi" (retry pakai payload yang sama, disimpan di `useRef`) dan link "Kembali ke form".
  - **✅ Resolved 2026-08-18 (Zufar, lewat merge)**: dua gap di atas dulu dicatat sebagai belum dikerjain, sekarang udah ketutup. `lib/api.ts` dapet class `ApiError extends Error` (nyimpen `status`) — `generateMindmap()` throw ini alih-alih `Error` polos. `app/api/mindmap/generate/route.ts` (backend) sekarang ngecek `parsedData.error === "REJECTED"` (Gemini diinstruksiin di `lib/aiPrompt.ts` buat balikin `{"error":"REJECTED"}` kalau topic-nya berbahaya/gak relevan) → `400` dengan pesan jelas. `app/loading/page.tsx` sekarang punya 3 state (`"loading" | "error" | "rejected"`, bukan cuma 2) — `err instanceof ApiError && err.status === 400` → `"rejected"` → tombol "Perbaiki Input" balik ke `/composer` (bukan retry-payload-sama); status lain → `"error"` → pesan "Sistem sedang sibuk..." + tombol "Coba Lagi" yang emang cocok buat error transient. Composer (`app/composer/page.tsx`) juga dapet `validateFile()` — ngecek ukuran (maks 10MB) dan ekstensi (`.pdf`/`.docx`/`.txt`) SEBELUM file ke-set ke state, dipanggil dari `handleFileChange` dan `handleDrop` dua-duanya, pesan error tampil di `.field-error` di bawah area upload.
- [x] Banner `feasibilityWarning` di atas canvas — `app/canvas/[id]/page.tsx` nampilin banner clay-tint (ikon warning + teks + tombol tutup) tepat di bawah header, cuma muncul kalau `mindmap.feasibilityWarning` gak `null` (field udah ada di kontrak wire, diisi Gemini pas generate). Dismiss-nya `useState` lokal (`showFeasibilityWarning`), gak persisted — reset otomatis tiap mindmap baru dimuat karena page-nya remount. Sengaja gak pakai warna kuning/amber (gak ada di token brand §4) — pakai `--sm-clay-tint` sebagai nada "heads up" yang lebih lembut dari `--destructive` (merah, disimpan buat aksi destruktif/error beneran). Diverifikasi lewat Playwright route interception (inject `feasibilityWarning` ke response `GET /api/mindmap/:id`, karena gak bisa kontrol kapan Gemini beneran nge-flag topik) — banner muncul dan tombol tutup jalan.
- **Gotcha yang kena pas implementasi** (dicatat buat referensi M7, polanya bakal muncul lagi): React Strict Mode di dev **double-invoke** `useEffect` — efek mount `app/loading/page.tsx` yang manggil `takePendingGenerateInput()` (queue "ambil sekali abis itu kosong") kena panggil dua kali, panggilan kedua dapat `null` dan langsung `router.replace("/composer")`, balapan sama hasil generate yang asli. Fix: guard `useRef` (`hasStartedRef`) di dalam efek supaya badan efek cuma bener-bener jalan sekali walau React manggil fungsinya dua kali.

### M7 — Alur elaborate
- [x] Input chat mengirim node yang sedang dipilih (chip konteks) — **wired ke `POST /api/chat` beneran** (endpoint baru dari partner, masuk lewat merge `development`→`frontend`, kontrak `{ mindmapId, message, nodeId? } → { message }`). Ada dua form chat di UI (drawer's `.ask-ai` dan canvas's `.command-bar`), sengaja **tetap dua elemen terpisah** (bukan digabung jadi satu) — desain yang dikonfirmasi user: submit dari drawer bukan bikin percakapan sendiri di drawer, tapi "handoff" — pesan + info node yang lagi dipilih dikirim ke `sendChatMessage()` (baru, `lib/api.ts`), balasannya dirender di `.chat-history` milik command-bar canvas, dan drawer langsung nutup diri sendiri (manggil `onClose()` yang udah ada, isinya `clearSelection()`). Drawer nutup ini yang bikin command-bar canvas otomatis muncul lagi, karena kondisi visibility `{!selectedNodeId && ...}` di `CanvasView.tsx` **gak perlu diubah sama sekali** — itu udah pas buat skenario handoff dari awal.
  - State chat (`chatMessages`, `isChatSending`, `chatError`) diangkat ke `app/canvas/[id]/page.tsx` (pola yang sama kayak `mindmapTopic`/`mindmapCreatedAt` — §7 poin 1: state suplemen lewat props dari page.tsx, bukan nambah ke canvasStore), diteruskan ke `CanvasView` (buat render+kirim) dan `Drawer` (buat kirim doang, gak render). `ChatMessage` type (`lib/api.ts`) punya field `nodeLabel?: string` yang **UI-only** — dipakai buat render chip kecil "Konteks: {label}" di atas bubble user yang berasal dari drawer, bukan bagian response backend (pola sama kayak `num`/`timeMark` di `MindmapNodeData`, §5).
  - Bubble user yang dikirim langsung dari command-bar canvas (tanpa node dipilih) **gak** punya chip konteks — `nodeId`/`nodeLabel` cuma keisi kalau pesannya lewat drawer.
  - **Keterbatasan yang sengaja gak di-fix di sini**: backend belum expose endpoint buat re-fetch riwayat `Message` (cuma ada `POST /chat`, gak ada `GET`). Jadi `chatMessages` cuma hidup di state React `page.tsx` — reset total tiap reload/pindah halaman. Ini kontrak asli backend saat ini (dicek langsung ke `app/api/chat/route.ts` — cuma nge-export `POST`), bukan bug yang kelewat, dan bukan sesuatu yang bisa dibenerin dari sisi frontend.
  - Diverifikasi end-to-end lewat Playwright (akun asli, generate mindmap asli lewat Gemini): klik node → isi chat di drawer → submit → drawer nutup otomatis → bubble user dengan chip "Konteks: {label node}" muncul di command-bar canvas → balasan AI beneran nongol (bukan mock). Lalu kirim pesan langsung dari command-bar canvas (gak ada node dipilih) → bubble baru tanpa chip konteks, riwayat sebelumnya tetap ada, balasan AI ke-2 juga beneran nongol. Reload halaman → riwayat chat kosong lagi (konfirmasi keterbatasan di atas, sesuai ekspektasi, bukan regresi).
  - **Fix susulan (user nemu lewat screenshot)**: balasan AI dari `getChatSystemPrompt()` (`lib/aiPrompt.ts`, backend) memang markdown mentah ("Return standard conversational markdown text") — awalnya cuma dirender sebagai `<p>{content}</p>` polos, jadi `**bold**` dan list bernomor kelihatan literal tanda bintangnya di bubble. Fix: pasang `react-markdown` (baru, dependency pertama buat markdown di project ini — dipilih di atas alternatif regex-manual karena dia render lewat AST bukan `dangerouslySetInnerHTML`, jadi gak ada risiko XSS dari konten yang datangnya dari Gemini), dipakai khusus buat bubble `role === "assistant"` (`.chat-message-markdown` di `CanvasView.tsx`) — bubble user tetap `<p>` polos karena itu teks ketikan sendiri, bukan markdown. CSS baru buat elemen hasil render (`p`, `ul`/`ol`, `strong`, `code` di dalam `.chat-message-markdown`). Diverifikasi lewat Playwright: response asli dari Gemini yang diminta pakai list bernomor + bold → `<strong>` beneran ada di DOM, gak ada `**` literal di teks yang kebaca browser.
- [x] Node baru masuk lewat `appendNodes`, lalu `applyLayout` dipanggil ulang — `store/canvasStore.ts` sekarang punya dua action baru: `appendNodes(newNodes, newEdges)` (nambahin ke array `nodes`/`edges` yang ada, tanpa re-layout) dan `applyLayout()` (manggil ulang `getLayoutedElements(state.nodes, state.edges)` dari `lib/canvas/layout.ts` yang sama dipakai `setGraph`, jadi cabang baru ikut di-fan ulang bareng cabang lama di step yang sama).
  - **Contoh pemakaian nyata**: tombol "Pecah jadi sub-cabang" di `Drawer.tsx` (dulu no-op) sekarang manggil `elaborateNode()` lalu `appendNodes()` + `applyLayout()`. Dikasih state `isExpanding` (teks tombol jadi "Memproses…", disabled selagi jalan) dan `expandError` (pesan `.field-error` kalau gagal — class baru, alias ke style yang sama kayak `.todo-form-error` tapi generik gak nyebut "todo", dipisah biar gak aneh dipake di luar konteks form to-do).
- [x] Viewport jangan lompat setelah node bertambah — gak butuh kode tambahan sama sekali, ternyata udah otomatis aman: React Flow cuma manggil ulang `fitView` pas mount awal (gotcha yang udah dicatat di §9 M1), dan `appendNodes`/`applyLayout` cuma nge-set ulang `nodes`/`edges` di store, gak ada kode yang manggil `fitView()` lagi atau remount `<CanvasView />`. Diverifikasi lewat Playwright: pan viewport ke posisi custom → baca `.react-flow__viewport`'s inline `transform` → klik "Pecah jadi sub-cabang" → baca transform lagi → **identik persis** sebelum dan sesudah, plus jumlah node nambah tepat 1.
- [x] `elaborateNode()` di-wire ke backend asli — ternyata `POST /api/mindmap/elaborate` udah lama ada dan lengkap (bahkan punya test suite sendiri, `app/api/mindmap/elaborate/__tests__/route.test.ts`), kontraknya persis sama kayak yang udah diasumsikan mock (`{ nodeId, concept, action, language } → { newNodes, newEdges }`, dikonfirmasi ke `ElaborateMindmapSchema` di `lib/validations.ts`) — tinggal ganti isi fungsi dari mock jadi `fetch()` beneran, **gak ada perubahan UI sama sekali** (`Drawer.tsx` udah lengkap dari awal). Response wire (`newNodes: WireMindmapNode[]`) di-map ke `MindmapNode[]` pakai pola yang sama kayak `toMindmap()` — `position` placeholder `{x:0,y:0}` (di-timpa `applyLayout()` lewat dagre) dan `timeMark` dihitung dari `timeOffsetDays` pakai `formatTimeOffset()` yang udah ada, tapi **gak ngisi `num`** (beda dari `toMindmap()`) karena kontrak elaborate cuma pernah balikin `mindmap-branch`, gak ada `roadmap-step` yang perlu dinomorin. `delay()`/`MOCK_LATENCY_MS` di `lib/api.ts` dihapus sekalian — itu terakhir dipakai fungsi mock ini, gak ada pemanggil lain. Diverifikasi lewat Playwright (akun asli, mindmap asli): klik "Pecah jadi sub-cabang" → network request ke `/api/mindmap/elaborate` beneran kekirim & balik `200` → node baru (4 cabang dari satu kali panggilan Gemini) muncul di canvas, gak ada `expandError`.
  - **Bug ketemu user, langsung nyusul**: cabang baru dari expand ilang kalau di-refresh. Root cause: `handleExpand()` di `Drawer.tsx` cuma manggil `appendNodes()`+`applyLayout()` ke store lokal — gak pernah `saveMindmap()`, jadi perubahannya cuma hidup di memory browser, gak pernah nyampe MongoDB. Fix: abis `appendNodes`/`applyLayout`, baca state ter-update dari `useCanvasStore.getState()` lalu panggil `saveMindmap(mindmapId, { nodes: updatedNodes, edges: updatedEdges })` (pola sama kayak `handleSaveTitle`). **Error message dipisah jadi 2 kasus** (beda dari pola rename yang cuma 1 pesan generik): kalau `elaborateNode()` (panggilan Gemini) sendiri yang gagal → pesan lama "Gagal memecah jadi sub-cabang. Coba lagi." (belum ada apa-apa yang berubah di canvas). Kalau Gemini-nya sukses tapi `saveMindmap()` yang gagal (node baru **udah kelanjur muncul** di canvas) → pesan beda, "Cabang baru muncul tapi gagal disimpan — refresh bisa menghilangkannya. Coba lagi." — sengaja dibedain karena kalau dikasih pesan generik yang sama, user bisa salah paham gak ada yang kejadian padahal cabangnya udah nongol, dan klik "coba lagi" bakal manggil Gemini lagi (bikin cabang duplikat/beda) alih-alih cuma retry simpan. Diverifikasi lewat Playwright: klik expand → `PUT /api/mindmap/:id` beneran kekirim (`200`) abis node baru nongol → **reload** → jumlah node tetap sama (gak balik ke jumlah sebelum expand).

### M8 — To-do dan reminder
- [x] Panel to-do per mindmap (tab kedua di sidebar) — `Drawer.tsx` sekarang punya tab bar ("Detail" / "To-Do") di `drawer-head`, gantiin label statis "Detail node". **Perubahan struktural**: Drawer gak lagi `return null` kalau gak ada node dipilih — visibility-nya sekarang dikontrol `app/canvas/[id]/page.tsx` lewat `isSidebarOpen = !!selectedNodeId || todoPanelOpen` (state baru), jadi sidebar bisa kebuka buat lihat to-do walau lagi gak ada node yang diklik. Tombol header baru "To-Do" (`sm-btn sm-btn--ghost`, sejajar Bagikan/Export) buka drawer langsung ke tab To-Do. Klik node di canvas otomatis balik ke tab "Detail" (state `sidebarTab` disesuaikan langsung pas render — bukan lewat `useEffect`, ngikutin pola React "adjusting state during prop change" biar gak kena lint `react-hooks/set-state-in-effect`). Tombol tutup (X) sekarang nutup dua-duanya sekaligus (`clearSelection()` + `setTodoPanelOpen(false)`), gak peduli tab mana yang lagi aktif.
- [x] Bikin task dari node — komponen baru `components/canvas/TodoPanel.tsx`, kalau ada node yang lagi dipilih pas buka tab To-Do, muncul chip "+ Tambah dari node "​{label}"" yang prefill input taskText pakai label node itu (user masih bisa edit sebelum submit).
- [x] Date picker due date, checkbox selesai — `TodoPanel.tsx` pakai `<input type="date">` native (bukan komponen shadcn baru, biar gak nambah risiko Tailwind-JIT-miss buat class yang belum pernah dipakai) dan `<input type="checkbox">` di-style lewat `accent-color: var(--sm-clay)`. Checkbox toggle optimistic (langsung keliatan di UI, revert kalau `updateTodo()` gagal).
  - **Fix kecil setelah dicoba**: submit form tambah-todo tanpa isi nama task atau due date awalnya silent-fail (gak ada feedback, tombol "Tambah" kelihatan gak ngapa-ngapain). Sekarang `handleSubmit` nampilin pesan spesifik ("Isi dulu nama tasknya." / "Pilih due date dulu.") lewat `.todo-form-error`, dan otomatis ke-clear begitu user mulai ngisi input yang kosong tadi.
- [x] Tandai visual node yang sudah punya task — dikerjain Zufar (masuk lewat merge `development`→`frontend` 2026-08-18), **tapi pakai pendekatan yang sama persis kayak yang udah diwanti-wanti rapuh di atas**: `RoadmapStepNode.tsx`/`MindmapBranchNode.tsx` baca `useCanvasStore(s => s.todos.some(t => !t.isCompleted && t.taskText === data.label))` — cocokin `taskText` ke `label` node secara teks, bukan lewat `nodeId` asli (yang emang masih belum ada di `WireTodo`). Kalau ketemu, node dapet titik oranye kecil (`.xf-pending-dot`) + border `--sm-clay-bright` (class `.has-pending-tasks`). Todos sendiri dipindah dari state lokal `TodoPanel.tsx` ke `canvasStore` (`todos`, `todosStatus`, `fetchTodos`/`setTodos`/`addTodo`/`updateTodoInStore`) — dimuat sekali di `app/canvas/[id]/page.tsx` (`fetchTodos(id)` di `useEffect` yang sama kayak `getMindmap`) biar node manapun bisa akses tanpa prop-drilling. **Keterbatasan lama masih berlaku**: kalau user ganti nama todo manual jadi beda dari label node manapun, dot-nya ilang walau task-nya masih ada; belum ada rencana benerin sampai `WireTodo` beneran dapet field `nodeId`.

**`getTodos`/`createTodo`/`updateTodo` udah dimigrasi dari mock ke fetch asli** (nyusul saran partner soal fitur auto-generate to-do di bawah, sekalian nutup gap migrasi M11 yang sempat ditunda):
- `lib/api.ts`: ketiga fungsi sekarang manggil `GET /api/todo?mindmapId=`, `POST /api/todo`, `PUT /api/todo/:id` beneran. `CreateTodoInput` berubah dari `{ mindmapId, taskText, dueDate }` jadi `{ mindmapId, taskText, timeOffsetDays: number | null }` — ngikutin kontrak backend asli yang ke-temu pas eksplorasi sebelumnya (`CreateTodoSchema` di `lib/validations.ts` konfirmasi `timeOffsetDays` optional+nullable). Backend yang hitung `dueDate = mindmap.createdAt + timeOffsetDays` sendiri.
- `todosDb`/`MOCK_TODOS`/`lib/mock/todo.ts` dihapus total — gak ada pemanggil lagi.
- **Field baru di wire type**: `WireMindmap.createdAt: string` ditambahin ke `lib/types.ts` — dikonfirmasi bukan cuma nebak, tapi lewat baca langsung `models/Mindmap.ts` (`createdAt: { type: Date, default: Date.now }`, field eksplisit di schema, bukan Mongoose `timestamps:true` implisit) dan `app/api/mindmap/[id]/route.ts` (`NextResponse.json(mindmap)` — seluruh dokumen dikirim, termasuk `createdAt`). `MindmapSummary` di `lib/api.ts` disederhanain jadi `Pick<WireMindmap, ... | "createdAt">`, gak perlu lagi intersection field terpisah kayak sebelumnya.
- **Form manual tambah-todo** (`TodoPanel.tsx`) tetap pakai `<input type="date">` (UX gak berubah, user tetap milih tanggal absolut) — tapi sekarang dikonversi ke `timeOffsetDays` relatif ke `mindmap.createdAt` (`dateToOffsetDays()`) sebelum dikirim ke `createTodo()`. `mindmapCreatedAt` diteruskan sebagai prop baru: `page.tsx` → `Drawer` → `TodoPanel`.
- **Fitur baru: tombol "Auto-generate N to-do dari roadmap"** — sesuai saran partner (gak butuh Gemini tambahan): loop `nodes` yang `type === "roadmap-step"` (dibaca langsung dari `useCanvasStore` di `TodoPanel.tsx`, bukan lewat prop), lalu `Promise.allSettled` bikin satu `createTodo()` per step pakai `data.label` sebagai `taskText` dan `data.timeOffsetDays` apa adanya (backend udah terima `null`). Style pakai class `.expand-btn` yang udah ada (dashed clay border), gak nulis CSS section baru. **Proteksi duplikat best-effort**: node yang labelnya udah persis sama dengan salah satu `taskText` todo yang ada di-skip dari batch — bukan solusi sempurna (masih rawan kalau user ganti nama todo manual jadi sama kayak label node lain), tapi cukup buat kasus normal (klik tombolnya berkali-kali gak bikin duplikat). Tombol otomatis disabled + ganti teks jadi "Semua langkah roadmap udah punya to-do" kalau gak ada step yang tersisa buat di-generate — `.expand-btn:disabled` (opacity + cursor) baru ditambahin ke CSS, sebelumnya gak ada state disabled sama sekali di situ.
- Diverifikasi end-to-end lewat Playwright: generate mindmap asli → tab To-Do kosong (beneran dari `GET` asli, bukan array mock) → klik auto-generate → 5 todo kebuat sesuai 5 `roadmap-step` dengan due date yang bener (dihitung dari `timeOffsetDays`) → tombol auto-generate disabled abis semua step ke-cover → tambah manual dengan tanggal custom → toggle checkbox → **reload halaman** → semua todo dan status selesainya tetap ada (konfirmasi beneran persisted ke MongoDB, bukan lagi in-memory mock yang reset).
- **List udah ke-sort by `dueDate` ascending** (`sorted = [...todos].sort(...)`, udah ada sejak awal, bukan tambahan baru) — dikonfirmasi ke user pas ditanya. Tambahan atas permintaan user: item yang tanggalnya **udah lewat** (`new Date(t.dueDate) < now`) tetap di posisi atas (ngikutin urutan tanggal alami, gak dipindah paksa), dipisah dari item yang akan datang pakai divider berlabel di tengah list (`.todo-divider`, teks "Sudah lewat tenggat"), muncul cuma kalau dua-duanya (`overdueItems` dan `upcomingItems`) sama-sama ada isinya.
  - **Iterasi pertama** (reuse `.drawer-divider`, garis polos 1px warna `--sm-line` tanpa label) ditolak user — kekontrasannya sama kayak border kartu to-do biasa, jadi nyaris gak keliatan sebagai pemisah section, bukan cuma gap antar-item biasa.
  - **Iterasi kedua** (disetujui lewat pilihan eksplisit): class baru `.todo-divider` — garis pakai `::before`/`::after` (2px, warna `--sm-ink-45`, lebih gelap dari `--sm-line`) di kiri-kanan label kecil uppercase, pola "section divider with label" standar. Tapi posisi awalnya SALAH: label "SUDAH LEWAT TENGGAT" ditaruh **di antara** dua kelompok (nutup `overdueItems`, buka `upcomingItems`) — user nunjukin screenshot nyata di mana item tanggal 25 Agu/September (jelas belum lewat) muncul persis di bawah label itu, kebaca seolah label itu judul buat item-item yang BELUM lewat (terbalik).
  - **Iterasi final (fix)**: restrukturisasi jadi dua header terpisah, masing-masing di ATAS kelompoknya sendiri — `.todo-divider` "Sudah lewat tenggat" muncul SEBELUM `overdueItems` (kalau ada), lalu `.todo-divider` "Akan datang" muncul SEBELUM `upcomingItems` (cuma kalau dua-duanya sama-sama ada isinya). Gak ada lagi ambiguitas arah baca. Diverifikasi lewat Playwright dengan skenario yang sama persis kayak screenshot user (1 item lewat tenggat + beberapa item masa depan) — struktur JSON dan screenshot ter-crop `.todo-list` konfirmasi label sekarang nempel bener ke kelompok yang dimaksud.

### M9 — Share dan export
- [x] Export PNG (`toPng` + `getNodesBounds` dari reactflow) — `components/canvas/ExportButton.tsx`, tombol Export di header
- [x] Export PDF — dialog export sekarang nawarin 2 tombol format ("PNG"/"PDF") gantiin satu tombol "Export". Library baru `jspdf` (client-side murni, gak butuh backend), `pdf.addImage()` nempel screenshot ke satu halaman yang ukurannya ngikutin aspect ratio canvas asli (`unit: "px"`, `format: [width, height]`) — bukan kertas standar A4/Letter, biar roadmap yang lebar/panjang gak kepotong atau nyisain white space.
  - **Gotcha ukuran file**: percobaan pertama nempelin PNG hasil `toPng()` langsung ke `addImage()` menghasilkan PDF 10.7MB dari sumber PNG 256KB (~40x lebih gede) — jsPDF nge-decode ulang data PNG jadi raw bitmap terus flate-compress sendiri, gak reuse kompresi PNG aslinya. Fix: khusus buat jalur PDF, capture pakai `toJpeg()` (bukan `toPng()`) dengan `quality: 0.92`, embed sebagai `"JPEG"` bukan `"PNG"` — hasil akhir ~104KB, sebanding sama PNG-nya, tetap tajam buat teks/garis roadmap. Jalur PNG murni gak kena perubahan ini (tetap `toPng()`, lossless).
  - Diverifikasi lewat Playwright (akun asli, generate mindmap asli): klik Export → pilih PDF → file `.pdf` ke-download dengan ukuran wajar (~100KB-an, bukan puluhan MB) → pilih PNG → file `.png` ke-download juga normal.
- [x] Toggle publik → tampilkan URL share + tombol copy — `components/canvas/ShareButton.tsx` (baru), dipicu tombol "Bagikan" di header canvas (`app/canvas/[id]/page.tsx`). Isinya `Switch` (shadcn, `npx shadcn add switch` — primitive `@base-ui/react/switch`, baru dipasang pertama kali) buat toggle `isPublic`, dan kalau publik nampilin input read-only berisi URL share + tombol salin (`navigator.clipboard`). **Gotcha Base UI**: elemen switch-nya dirender sebagai `<span role="switch">`, bukan `<button>` — kepake pas nulis test, gak ngaruh ke pemakaian normal.
  - `saveMindmap()` di `lib/api.ts` **sudah** manggil `PUT /api/mindmap/:id` beneran, input-nya diperluas jadi `{ nodes?, edges?, isPublic? }` (semua optional, cocok sama backend) — sebelumnya cuma nerima `nodes`/`edges` wajib padahal belum pernah dipanggil komponen manapun. **Gotcha kontrak**: respons `PUT` cuma `{ success: true, message }`, **gak balikin** dokumen yang ke-update (jadi `shareId` baru yang di-generate backend gak langsung kebaca dari situ) — makanya `ShareButton` manggil `getMindmap(id)` lagi tepat setelah `saveMindmap()` sukses, buat nangkep `shareId` barunya.
  - Sekalian beres-beres: `mindmapsDb`/`findMindmapOrThrow`/`MindmapRecord` di `lib/api.ts` dihapus — udah gak ada pemanggil sama sekali sejak `deleteMindmap`/`getMindmap`/`getMindmaps` semua pindah ke fetch asli. `todosDb` dan `DEFAULT_MINDMAP_ID` tetap ada (masih kepake).
- [x] Halaman `/share/[shareId]` (baru) — konsumsi `getSharedMindmap()` (fungsi baru di `lib/api.ts`, `GET /api/mindmap/share/:shareId`, gak butuh session). Header beda dari canvas biasa: gak ada tombol back/account chip (gak ada sesi buat visitor publik), badge "Tampilan publik" (ikon mata + teks) nempel langsung di sebelah judul (`.doc-info-title-row`) — bukan lagi pill sendirian di kanan header, ditambah tombol CTA "Buat mindmap sendiri" yang **udah dicabut lagi** (lihat catatan perbaikan di bawah). **Keputusan scope (awal)**: reuse `CanvasView` (pan/zoom/fit tetap jalan) tapi **tanpa** `Drawer` — Drawer punya input judul yang bisa diedit, tombol "tandai selesai", zona AI, dan (belakangan) tab To-Do, semua gak pantas buat visitor publik yang lihat mindmap orang lain; klik node di halaman share jadi gak ada efek visual (`selectedNodeId` ke-set di store tapi gak ada yang render-nya) — cukup buat MVP, bukan bug fatal.

**Update — panel read-only ditambahin**: user minta klik node di halaman share tetap nampilin sesuatu, khusus deskripsi node-nya doang. Daripada nambah flag "public mode" ke `Drawer.tsx` (yang bakal maksa hampir seluruh body-nya dibungkus kondisional — title-input, complete-btn, notes, siblings, AI zone, tab bar — jadi rawan berantakan), dibikin komponen terpisah `components/canvas/PublicNodePanel.tsx`: baca `selectedNodeId`/`nodes` dari `canvasStore` yang sama, tapi cuma render label (`.node-title-static`, class baru — typografi sama kayak `.node-title` tapi teks statis, bukan input) + tipe node + `Ringkasan`/deskripsi. Gak ada tombol edit/selesai/AI/to-do sama sekali. `app/share/[shareId]/page.tsx` render `<PublicNodePanel />` di `.canvas-body` (sejajar `.canvas-area`, sama pola kayak canvas asli), dipicu `selectedNodeId` truthy, plus class `has-drawer` buat `.canvas-body` biar responsive <900px konsisten sama canvas asli. CSS baru: `.drawer-head-label` (reintroduce label eyebrow yang sempat kehapus pas `.drawer-head-title` diganti jadi tab bar di Drawer asli) dan `.node-title-static`. Diverifikasi lewat Playwright: toggle mindmap jadi publik → buka share link di browser context baru tanpa login sama sekali → klik node → panel muncul isinya cuma judul+ringkasan, dikonfirmasi gak ada `input.node-title`/`.complete-btn`/`.ai-zone`/`textarea`/`.drawer-tabs` di DOM-nya → tombol tutup (X) jalan.
  - **Perbaikan setelah user coba langsung**: (1) tombol "Bagikan" sekarang ada jeda loading 1.5 detik (`OPEN_DELAY_MS` di `ShareButton.tsx`, pola sama kayak `ExportButton`) sebelum dialog kebuka, biar berasa "nyiapin sesuatu" bukan instan. (2) **Bug nyata ketemu**: input URL share ketiban tombol "Salin" — akar masalahnya, `Dialog` (Base UI) di-*portal* ke luar `.page-canvas` (ke `document.body`), jadi rule CSS `.page-canvas .share-url-row`/`.share-url-input`/`.share-toggle-row` **gak pernah match sama sekali** (dead CSS), input-nya kerender polos tanpa box. Fix: rule-rule itu di-unscope dari `.page-canvas` (dikasih komentar penjelasan kenapa), plus nambah `text-overflow: ellipsis` di input biar URL panjang gak dobel sama tombol. (3) Tombol CTA "Buat mindmap sendiri" dicabut dari halaman share (kepenuhan/kurang perlu), badge "Tampilan publik" dipindah nempel ke judul + dikasih ikon `Eye` kecil biar gak keliatan nyendiri sebagai pill kosong di header. (4) Command bar chat ("Tanya atau perintahkan sesuatu...") sekarang disembunyiin khusus di halaman share — `CanvasView` dapet prop baru `showCommandBar?: boolean` (default `true`), dipanggil `<CanvasView showCommandBar={false} />` di halaman share aja; canvas asli tetap nampilin command bar-nya seperti biasa.
  - **Redesign header (round 2)**: header halaman share pakai `.canvas-header` yang sama kayak canvas asli — tinggi 56px fixed, `padding: 0 24px` (cuma horizontal, nol vertikal). Karena `.doc-info` di halaman share sekarang 2 baris (judul+badge, lalu subtitle) nyaris pas 52.78px, sisa ruang di header cuma ~1.6px atas-bawah — mepet banget, itu yang bikin kerasa "kurang rapih". Masalahnya `.canvas-header` dan `.canvas-body` (`height: calc(100vh - 56px)`) saling gantung di canvas asli, jadi gak bisa asal diubah tingginya (bakal ganggu canvas asli). Fix: halaman share dikasih class pembeda `.page-share` di wrapper terluar, override khusus di situ — `.page-share .canvas-header { height: 72px }`, `.page-share .canvas-body { height: calc(100vh - 72px) }` (ikut nambah biar tetap pas), plus `.page-share .doc-info { display:flex; flex-direction:column; gap:4px }` buat kasih jarak antara judul dan subtitle yang sebelumnya nempel tanpa gap. Diverifikasi: header share sekarang 72px dengan jarak atas-bawah ~10-11px (dari ~1.6px), canvas asli tetap persis 56px gak berubah sama sekali.
  - **Redesign badge "Tampilan publik" (round 3-4)**: mula-mula badge-nya kerender setinggi 25.8px — nyaris menyamai tinggi judul bold di sebelahnya (26.39px), kelihatan gembung/berat padahal cuma teks 11px. Akar masalahnya `.share-badge` mewarisi `line-height: 1.65` dari `body` (global, `app/globals.css`) — 11px × 1.65 ≈ 18px baris teks, ditambah padding, jadinya nyaris setinggi judul, tapi TIDAK TERKONTROL (ukurannya kebetulan, bukan didesain). Iterasi pertama: `line-height: 1` bikin badge jadi 19px — proporsional tapi user bilang kekecilan/kurang berasa. Iterasi kedua (final): font-size dinaikin ke `--sm-fs-small` (13.5px, token yang sama dipakai label form), padding dilebarin (`--sm-space-3` kiri-kanan), ikon `Eye` naik ke `size-3.5` (14px), `line-height:1` tetap dipertahankan biar tingginya terkontrol presisi (bukan produk sampingan inheritance). Hasil akhir: badge 25px (rasio 0.95 ke judul) — kali ini SENGAJA sebesar itu lewat font-size+padding yang didesain, bukan kebetulan dari line-height yang gak diatur, sesuai arahan user ("lebih besar yang masih proporsional").
  - Diverifikasi lewat Playwright penuh: generate → toggle publik → buka link dari context browser baru tanpa login → mindmap muncul; toggle balik privat → visitor yang sama kena pesan "Mindmap ini gak lagi dibagikan secara publik."

### M10 — PWA dan offline
- [x] **Konfigurasi `next-pwa` + manifest + ikon 2026-08-18** — dua blocker teknis nyata ketemu & dibenerin, dua-duanya baru kebukti lewat build+browser beneran, bukan cuma baca kode:
  1. **Turbopack vs webpack plugin.** Next.js 16 (dicek langsung `node_modules/next/dist/docs`) jadiin Turbopack default bundler buat `dev` DAN `build`, dan Turbopack **gak support hook `webpack()` di `next.config.js` sama sekali** — sementara `next-pwa` (udah lama jadi dependency, lihat M0) kerjanya PERSIS lewat situ (Workbox webpack plugin, generate `sw.js`+`workbox-*.js` pas `next build`). Dibiarin apa adanya, plugin-nya diem-diem gak ngefek. Ditanya ke user 2 opsi (paksa `--webpack` khusus build vs tulis service worker manual) — dipilih **opsi 1**: `package.json`'s `"build"` sekarang `"next build --webpack"` (`"start"`/`"dev"` gak berubah — `start` gak nge-build ulang, `dev` next-pwa emang udah `disable` sendiri lewat `disable: process.env.NODE_ENV === "development"` di `next.config.ts`, jadi Turbopack dev tetap full speed, gak ada dampak ke workflow harian). `next-pwa` gak punya `.d.ts` sendiri — `types/next-pwa.d.ts` baru (ambient module declaration) biar `tsc --strict` gak protes.
  2. **App Router vs Pages Router.** Bahkan setelah `--webpack` bikin `sw.js` beneran ke-generate (dikonfirmasi ada filenya, `ls public/sw.js`), tes langsung di browser (Playwright, bukan cuma baca kode) nunjukin `navigator.serviceWorker.getRegistration()` gak pernah keisi — next-pwa v5.6.0 fitur "auto register"-nya nyuntik lewat `_document.js`, konsep Pages Router yang gak ada padanannya di App Router (`app/layout.tsx`). Fix: `components/OfflineSupport.tsx` (baru, lihat di bawah) register manual (`navigator.serviceWorker.register("/sw.js")`), di-guard `NODE_ENV === "production"` biar gak nyoba register file yang emang gak ada pas dev.
  - **Manifest**: `app/manifest.ts` (konvensi native Next.js — `MetadataRoute.Manifest`, auto ke-link ke `<head>` sebagai `/manifest.webmanifest`, gak perlu sentuh `layout.tsx` metadata manual). Ikon udah lama ada di `public/brand/png/` (`icon-192.png`, `icon-512.png`, `icon-512-maskable.png`) — gak perlu generate aset baru sama sekali, tinggal dirujuk.
  - **Diverifikasi lewat browser beneran** (Playwright headless, bukan cuma baca kode): `npm run build` → log build eksplisit nunjukin `[PWA] Service worker: .../public/sw.js` → `ls public/sw.js` ada filenya → `npm run start` (server produksi asli, bukan dev) → `<link rel="manifest">` di `<head>` ngarah ke `/manifest.webmanifest`, fetch balik `200` isinya nama+3 ikon bener → `navigator.serviceWorker.register()` sukses, siklus `installing→activated` dalam ~500ms → reload → `navigator.serviceWorker.controller` kepasang (halaman beneran dikontrol service worker). `sw.js`/`workbox-*.js` ditambahin ke `.gitignore` (build artifact, regenerated tiap `next build --webpack`, bukan source).
  - Tombol "Install App" **belum dikerjain** — belum sempat, ditunda ke sesi berikutnya kalau dibutuhin (`beforeinstallprompt` event + tombol custom).
- [x] **Zustand `persist` buat cache mindmap + index dashboard 2026-08-18** — `store/canvasStore.ts` sekarang dibungkus `persist()` (localStorage, key `secondmind-canvas-cache`), `partialize` cuma nyimpen `mindmapId`+`nodes`+`edges`+`todos` (bukan `selectedNodeId`, state UI sesaat yang gak berguna disimpan lintas sesi). Field baru `mindmapId: string | null` nandain mindmap MANA yang lagi ke-cache — krusial buat validasi offline (lihat poin di bawah): kalau id yang lagi dibuka beda dari `mindmapId` yang ke-cache, cache-nya gak dipakai (ngehindarin nampilin data mindmap lain yang kebetulan terakhir dibuka, termasuk dari halaman share publik — `setGraph` di `app/share/[shareId]/page.tsx` juga nulis ke store yang sama, tapi karena dijaga lewat perbandingan id, gak ada risiko ke-tuker). `store/historyStore.ts` (baru) — store terpisah khusus buat list "Riwayat" dashboard (`MindmapSummary[]`), juga di-`persist` (key `secondmind-history-cache`) — sengaja dipisah dari `canvasStore` (bukan ditambahin sebagai field baru ke situ), ngikutin prinsip §7 poin 1 (metadata/list-level data gak dicampur ke store yang isinya detail-mindmap-yang-lagi-dibuka).
- [x] **Satu listener `online`/`offline` di root 2026-08-18** — `components/OfflineSupport.tsx` (baru), di-mount di `app/layout.tsx` sebagai child `AuthProvider` (butuh `useSession()`). Isinya 2 `useEffect`: (1) listener `window.addEventListener("online"/"offline", ...)` → `canvasStore.setIsOnline()` (field baru `isOnline: boolean`, default `true` — bukan langsung baca `navigator.onLine` biar gak mismatch SSR/hydration, dikoreksi client-side sesaat setelah mount); (2) register service worker manual (lihat poin next-pwa di atas).
- [x] **Disable aksi yang butuh network + banner mode offline 2026-08-18** — `isOnline` dibaca langsung dari `canvasStore` (selector spesifik, §7 poin 3) di tiap komponen yang butuh, bukan di-prop-drill: `Drawer.tsx` (simpan judul, tandai selesai, simpan catatan, hapus node, expand AI, kirim chat — tombol+guard di function body, dua-duanya, biar gak bisa ke-trigger lewat Enter/keyboard walau tombolnya kekunci visual), `CanvasView.tsx` (kirim chat command-bar, connect/reconnect/hapus edge — `nodesConnectable`/`edgesReconnectable` React Flow juga ikut di-`isOnline`), `ShareButton.tsx` (toggle publik — trigger tetap bisa dibuka buat lihat/salin link yang udah ada, cuma `Switch`-nya yang dikunci), `TodoPanel.tsx` (tambah/toggle/auto-generate to-do). Banner `.offline-banner` (baru, beda palet dari `.feasibility-banner` — netral paper+ink-45, bukan clay-tint, biar gak ketuker sama warning konten) muncul di `app/canvas/[id]/page.tsx` dan `app/composer/page.tsx`. CSS `:disabled` ditambahin buat elemen yang sebelumnya gak punya state visual disabled sama sekali (`.icon-btn`, `.complete-btn`, `.command-bar`/`.ask-ai` button, `.history-delete`, dll) — sebelum ini diklik pas disabled kelihatan gak ngapa-ngapain tanpa penjelasan visual.
- [x] **Wipe cache sebelum `signOut()` + cek `userId` saat boot 2026-08-18** — `lib/offlineCache.ts` (baru): `clearOfflineCache()` manggil `useCanvasStore.persist.clearStorage()` + `useHistoryStore.persist.clearStorage()` (API bawaan zustand `persist` middleware) + hapus marker `secondmind-cached-user-id` dari localStorage. Dipanggil di kedua titik tombol "Keluar" (`app/composer/page.tsx`, `app/page.tsx`) SEBELUM `signOut()`. Lapisan kedua (defense-in-depth, jaga-jaga titik pertama gak sempet jalan — sesi expired, tab ditutup paksa) di `OfflineSupport.tsx`: begitu `useSession()` resolve ke `authenticated`, `session.user.id` dibandingin ke marker yang ke-persist — beda → wipe otomatis sebelum marker di-update ke user baru.
- **Cache offline mindmap+riwayat, bukan cuma placeholder** — `app/canvas/[id]/page.tsx` sekarang punya fallback nyata: `getMindmap(id)` gagal DAN beneran offline (bukan gagal karena alasan lain kayak 404/sesi habis, itu tetap jatuh ke error biasa) DAN `mindmapId` yang ke-cache cocok sama `id` route → render dari `nodes`/`edges` yang ke-persist, metadata (`title`/`topic`/`timeframe`/dll) diambil dari entry yang cocok di `historyStore` (kalau ada — biasanya udah ke-cache duluan dari dashboard). `app/composer/page.tsx`'s "Riwayat" dapet status baru `"cached"` (beda dari `"ready"`) — muncul catatan kecil "nampilin riwayat tersimpan terakhir" biar gak keliatan kayak data live padahal offline.
- **✅ Diverifikasi live 2026-08-18** (susulan, user kasih akun test) — sebelumnya dicatat "belum sempat", sekarang udah beneran dites end-to-end lewat Playwright pakai akun asli (`testyoga@mail.com`, generate 1 mindmap sungguhan "Kuasai Dasar-dasar HTML dalam 1 Minggu", 17 node, manggil Gemini API beneran). Alur yang dites: login → generate → buka canvas (online) → putus koneksi (`context.setOffline(true)`, native `offline` event browser kebukti auto-fire, gak perlu dispatch manual) →
  - **Kebukti bener SEMUA**: banner offline muncul dengan teks yang bener; tombol Drawer (tandai selesai, hapus node, simpan judul, simpan catatan, kirim chat, pecah sub-cabang) semua ke-disable; `ShareButton`'s Switch ke-disable + catatan offline muncul; `TodoPanel`'s tombol submit ke-disable; command-bar input ke-disable; tombol "To-Do" di header TETAP enabled (bener, itu cuma buka panel, gak butuh network); pindah ke composer lewat klik link (bukan reload) → riwayat kebaca dari cache dengan catatan "Gak bisa nyambung ke server — nampilin riwayat tersimpan terakhir", tombol "Buat Mindmap" & tombol hapus riwayat ke-disable, tombol "Unggah File" TETAP enabled (bener, itu cuma milih file lokal).
  - **⚠️ Limitasi nyata ketemu, belum di-fix**: skenario di atas cuma jalan kalau navigasi antar-halaman terjadi LEWAT KLIK di dalam SPA yang udah ke-load (React Router App Router pakai fetch RSC payload, bukan full document request). **Hard reload / buka tab baru / ketik URL langsung pas offline TETAP gagal** — jatuh ke halaman error native browser (`chrome-error://chromewebdata`), walau di production build dengan service worker yang udah `activated` DAN halaman yang sama udah kebukti ke-cache duluan (dicek langsung isi `caches.open()`, path `/composer` dan `/canvas/<id>` ada di situ). Root cause: cache yang keisi itu dari RSC-payload prefetch fetch (dipicu `<Link>`), beda content-type/request-shape dari full-document `navigate`-mode request yang dikirim browser pas reload/direct-URL — `next-pwa`'s default runtime caching gak nangani `navigate`-mode request itu dengan fallback ke cache, jadi tetap network-only buat kasus itu. Fix beneran butuh config tambahan next-pwa's `fallbacks.document` (offline fallback page khusus) — di luar scope M10 checklist asli, dicatat di sini buat referensi kalau mau di-lanjutin.
  - **Implikasi praktis**: PWA ini sekarang beneran ngebantu kalau user LAGI BUKA app terus internetnya putus di tengah jalan (skenario paling umum — WiFi kafe kepapas kek, HP masuk mode pesawat sebentar) — sesuai motivasi awal fitur ini (pertanyaan "user login, diskoneksi, mau buka riwayat"). TAPI belum cocok buat skenario "buka app dari kondisi offline dari awal" (misal buka HP pagi-pagi belum konek WiFi, langsung tap ikon PWA) — itu masih bakal keliatan gagal load sampai koneksi nyambung lagi.
- [ ] Tombol "Install App" (`beforeinstallprompt`) — belum dikerjain, ditunda

### M11 — Halaman pendukung dan perapian
- [x] Halaman login (Google + email/password) — backend partner udah kelar sisi mereka: `lib/auth.ts` (Google OAuth + Credentials provider, auto-register user Google baru, session JWT ada `user.id`), `POST /api/auth/register`, `types/next-auth.d.ts`. Frontend: `components/AuthProvider.tsx` (`SessionProvider` wrapper) dipasang di `app/layout.tsx`. `app/register/page.tsx` wired ke `POST /api/auth/register` — validasi password vs konfirmasi di klien, banner error (`.form-error` di `auth.css`) buat email sudah terdaftar, sukses → redirect ke `/login`. `app/login/page.tsx` wired ke `signIn("credentials", { redirect: false })` — salah password nampilin `.form-error` "Email atau password salah" tanpa pindah halaman, sukses → redirect ke `/composer`; tombol "Masuk dengan Google" (`GoogleIcon` lokal, divider `.auth-divider`) manggil `signIn("google", { callbackUrl: "/composer" })`. Semua diverifikasi lewat Playwright end-to-end pakai akun asli di MongoDB Atlas (bukan mock): register→login salah password→login benar→sesi kebaca lewat `/api/auth/session`→klik Google beneran nyampe ke `accounts.google.com` dengan `client_id`/`redirect_uri` yang benar.
- [x] Proteksi route — `middleware.ts` (baru, root `next-app/`) pakai `withAuth` dari `next-auth/middleware`, `matcher: ["/composer/:path*", "/canvas/:path*", "/loading/:path*"]`, `pages.signIn: "/login"`. Belum login akses salah satu route itu → redirect `307` ke `/login?callbackUrl=<path-asal>`; `/`, `/login`, `/register` tetap publik. **Gotcha yang kena pas verifikasi**: `router.push(callbackUrl)` abis `signIn()` sukses kadang gak pindah halaman (~50% kejadian pas ditest beruntun) — root cause-nya Next.js App Router client-side router cache: kunjungan pertama ke `/composer` (sebelum login, kena redirect) ke-cache, jadi `router.push` abis login kadang masih mainin hasil cache lama alih-alih re-check middleware ke cookie session yang baru. Fix: `router.refresh()` dipanggil tepat setelah `router.push(callbackUrl)` di `app/login/page.tsx` buat bust cache-nya. Diverifikasi 5x percobaan login berturut-turut (context baru tiap kali) — konsisten berhasil.
- [x] Tombol logout — avatar di `app/composer/page.tsx` (dulu `<div className="avatar">AP</div>` statis) sekarang `DropdownMenu` (shadcn, `npx shadcn add dropdown-menu` — primitive `@base-ui/react/menu`, baru dipasang pertama kali di project ini) dipicu `DropdownMenuTrigger`. Isi dropdown: nama/email user asli dari `useSession()` (`DropdownMenuLabel`, inisial avatar dihitung dari `session.user.name`/`email` lewat `getInitials()`), lalu item "Keluar" (`variant="destructive"`) manggil `signOut({ callbackUrl: "/login" })`. **Gotcha Base UI vs Radix**: shadcn dropdown-menu yang biasa (Radix-based) bisa taruh `DropdownMenuLabel` langsung sebagai child `DropdownMenuContent`, tapi versi Base UI project ini nge-throw runtime error ("MenuGroupContext is missing") kalau gitu — `GroupLabel` wajib dibungkus `<DropdownMenuGroup>` dulu. Juga `asChild` (pola Radix buat custom trigger element) gak ada di Base UI; `DropdownMenuTrigger` render elemennya sendiri (button), tinggal kasih `className`/children langsung. Diverifikasi end-to-end: initial dropdown, klik "Keluar" → redirect ke `/login`, sesi kebukti kosong (`/api/auth/session` balik `{}`), akses `/composer` lagi abis logout → kepental ke `/login` (middleware tetap jalan).
  - **Styling dropdown dirapikan** — versi mentah hasil `shadcn add` kepadatannya gak nyambung sama rythm spacing app ini (`p-1`/`text-xs` Tailwind default) plus `rounded-lg` di project ini otomatis ke-remap jadi 22px (`--sm-radius-lg`, lihat §4) yang kegedean buat menu sekecil itu — kombinasinya kelihatan sempit dan gak matching. Diperbaiki di `components/ui/dropdown-menu.tsx`: radius diturunin ke `rounded-md` (12px), padding dilongarin (`p-1.5`, item `px-2 py-1.5`), border eksplisit `border-border` (bukan `ring-1`) biar konsisten sama pola border elemen lain, shadow diganti pakai token brand (`shadow-(--sm-shadow-md)`, sebelumnya Tailwind default shadow yang gak ke-remap ke token `--sm-shadow-*`). Label akun di composer sekarang dua baris (nama tebal + email `truncate` di bawahnya, bukan satu baris gabungan) — perlu `min-w-0` di flex child-nya biar `truncate` beneran motong, bukan malah maksa lebar. **Gotcha kecil**: sempat coba `min-w-64` (belum pernah dipakai di codebase ini) dan Tailwind gak generate CSS-nya sama sekali (`min-width` komputed jadi `0px`, ke-override sama `w-(--anchor-width)` dari Base UI yang ngikutin lebar trigger/avatar ~34px, bikin menu-nya collapse jadi sempit banget) — balik ke `min-w-56` (udah pernah dipakai & ke-generate) langsung jalan normal lagi. Kalau butuh lebar lain di masa depan, kemungkinan perlu restart dev server biar Tailwind JIT nangkep class barunya.
- [x] **Bug ketemu user**: landing page (`app/page.tsx`) nav bar-nya statis, gak pernah cek sesi — user yang udah login terus balik ke `/` tetap lihat tombol "Masuk"/"Coba gratis" kayak belum login sama sekali. Fix: `useSession()` (`next-auth/react`, kerja karena `AuthProvider`/`SessionProvider` udah wrap seluruh app di `app/layout.tsx`) — `status === "authenticated"` render tombol "Buka Composer" (`Link` ke `/composer`) gantiin pasangan Masuk/Coba gratis, `status === "unauthenticated"` render kayak semula, `status === "loading"` render kosong dulu (ngindarin flash tombol yang salah). Cuma nav bar atas yang dibenerin (itu yang dikomplain) — CTA lain di halaman ("Buat peta pertama" di hero dan section akhir, yang keduanya ke `/register`) sengaja belum disentuh, kemungkinan besar kena masalah serupa tapi di luar scope laporan ini.
  - **Susulan**: user minta bisa logout juga dari homepage (awalnya cuma ada tombol "Buka Composer", gak ada jalan keluar sesi). Ditambahin avatar `DropdownMenu` di sebelah tombol itu — pola & markup-nya sama persis kayak yang udah ada di `app/composer/page.tsx` (identitas akun + item "Keluar" destructive manggil `signOut()`), bedanya `callbackUrl` diarahin ke `/` (bukan `/login` kayak composer) karena homepage-nya publik, gak ada alasan maksa redirect abis logout. Helper `getInitials()` yang tadinya cuma ada lokal di `composer/page.tsx` dipindah ke `lib/utils.ts` (dipakai 2 tempat sekarang, gak diduplikasi). CSS `.avatar` juga tadinya cuma di-scope ke `.page-composer` (`composer.css`) — ditambahin versi `.page-landing .avatar` yang sama persis di `landing.css`, karena Tailwind/CSS scoping project ini emang per-halaman (§4 catatan token, pola udah konsisten dari M1). Diverifikasi lewat Playwright: dropdown nampilin nama+email asli dari `useSession()`, klik "Keluar" → nav balik ke Masuk/Coba gratis → `GET /api/auth/session` beneran kosong (bukan cuma UI yang keliatan logout).
  - Diverifikasi lewat Playwright: logged-out → nav tampil Masuk+Coba gratis seperti biasa; logged-in → nav tampil "Buka Composer" + avatar, klik "Buka Composer" beneran nyampe ke `/composer`.
- [x] **Bug ketemu user**: teks tombol "Buat peta pertama" di section akhir landing page (`.final`, `app/page.tsx`) kelihatan samar. Root cause: `.sm-section-dark a { color: var(--sm-clay-bright) }` (`app/globals.css`) — spesifisitasnya (class+element) lebih tinggi dari `.sm-btn--primary { color: #fff }` (satu class doang), jadi warna tombol ke-override jadi `--sm-clay-bright` (`#E0794F`, oranye terang) di atas background `--sm-clay` (`#C75B39`, oranye kemerahan) — dua warna oranye yang mirip, kontrasnya rendah, kebaca "samar". Tombol yang sama di hero (bukan di dalam `.sm-section-dark`) gak kena karena gak ada rule pesaing di situ. Fix: tambah override eksplisit `.sm-section-dark a.sm-btn--primary/--dark/--ghost` di `globals.css` biar tombol apa pun yang nangkring di section gelap tetep pakai warna teks sesuai variant-nya sendiri, bukan warna link generik section itu. Diverifikasi: `getComputedStyle` konfirmasi tombol di section akhir sekarang `color: rgb(255,255,255)` (putih, sama kayak tombol di hero), plus screenshot visual.
- [x] Dashboard daftar mindmap + tombol hapus — `app/composer/page.tsx` "Riwayat" baca `getMindmaps()`, link tiap item ke `/canvas/${item._id}` (route `app/canvas/[id]/page.tsx`, `getMindmap(id)` dipanggil per-id lewat `useParams`). `app/canvas/page.tsx` (tanpa id) redirect ke `DEFAULT_MINDMAP_ID`. Tombol hapus per item (ikon trash) buka dialog konfirmasi (pola sama kayak `ExportButton`, pakai `Button variant="destructive"`) → panggil `deleteMindmap()` → hapus dari state lokal.
  - **Bug ketemu & di-fix**: abis generate mindmap terus balik ke composer pakai **tombol back browser**, mindmap barunya kadang gak nongol di "Riwayat" sampai di-reload manual. Bukan soal Next.js router cache (dicoba navigasi via `<Link>` biasa, itu udah fetch ulang dengan benar) — ternyata browser's **back-forward cache (bfcache)**: tombol back memulihkan halaman dari snapshot beku tanpa JS jalan ulang sama sekali, jadi `useEffect` mount gak pernah ke-trigger lagi, nyangkut nampilin state lama (sebelum generate). Fix: listener `pageshow` yang ngecek `event.persisted` — kalau `true` (artinya halaman dipulihkan dari bfcache), `getMindmaps()` dipanggil ulang. Diverifikasi lewat Playwright: generate → `page.goBack()` beneran (bukan simulasi) → mindmap baru langsung muncul tanpa reload manual.
- [x] Empty state dan loading skeleton — `app/loading/page.tsx` punya skeleton bar; dashboard "Riwayat" sekarang juga punya skeleton row + empty state (`.history-empty`) pas `getMindmaps()` kosong/gagal
- [x] Layar kecil: sidebar jadi bottom sheet — sebelumnya di `<900px` drawer cuma nge-swap total (`.canvas-area { display: none }`, drawer jadi full-width/full-height kayak halaman baru, bukan sheet beneran). Sekarang: canvas TETAP kelihatan di belakang (redup lewat `.drawer-backdrop` baru, overlay `rgba(--sm-ink, 0.45)` — klik backdrop nutup drawer, `onClose` yang sama kayak tombol X), drawer jadi `position: fixed` nempel di bawah viewport (`max-height: 82vh`, rounded top corners `--sm-radius-lg`), plus `.drawer-handle` (bar kecil abu-abu di tengah atas, indikator visual doang — belum ada drag-to-dismiss gesture beneran, cuma tap backdrop atau tombol X). Animasi slide-up (`@keyframes drawer-sheet-slide-up`, `translateY(100%)→0`) + fade-in backdrop, jalan otomatis pas drawer di-mount (gak butuh JS timing, murni CSS `animation` di elemen yang baru muncul). `Drawer.tsx` dibungkus fragment (`<>`) buat nampung `.drawer-backdrop` sebagai sibling elemen sebelum `<aside className="drawer">` — backdrop-nya sendiri `display:none` di desktop (gak ada dampak ke layar gede). Diverifikasi lewat Playwright (resize ke viewport 390×844): canvas kebukti masih kebaca di belakang, drawer nempel presisi di bawah viewport, handle+backdrop kelihatan cuma di mobile (gak leaking ke desktop), klik backdrop beneran nutup drawer.
- [x] **Mobile responsive pass menyeluruh 2026-08-18** — disurvey dulu semua halaman (`landing.css`, `auth.css`, `composer.css`, `canvas.css`, plus share page yang reuse canvas.css), ketemu 5 gap:
  1. **Header canvas** (`.header-actions` — tombol To-Do/Bagikan/Export) — 3 tombol teks penuh + judul dokumen dalam satu flex row tanpa wrap (`flex-shrink:0`), berisiko overflow horizontal di ≤560px. Fix: tiap tombol (`app/canvas/[id]/page.tsx`, `ShareButton.tsx`, `ExportButton.tsx`) dikasih ikon (`lucide-react`: `ListChecks`/`Share2`/`ImageDown`) + teks dibungkus `<span className="btn-label">`; di `≤560px` (`canvas.css`) `.btn-label` di-`display:none`, padding tombol dikecilin jadi persegi mirip `.icon-btn` — sisa ikon doang. `aria-label` ditambah di tiap tombol biar labelnya tetap kebaca screen reader walau teksnya disembunyiin visual.
  2. **Nav landing** — link tengah (`Cara kerja`/`Fitur`/`Contoh peta`) hilang total tanpa pengganti di `≤860px` (gap lama, udah dicatat pas survey M11 dulu tapi gak pernah di-follow-up). Fix: hamburger button baru (`Menu`/`X` dari `lucide-react`) di `app/page.tsx` buka `.mobile-nav-panel` (dropdown absolute di bawah nav, isinya 3 link yang sama) — klik link atau toggle nutup panel lagi.
  3. **Bug nyata ketemu pas testing hamburger di atas** (independen dari perubahan #2, udah ada dari awal) — diverifikasi lewat script Playwright (headless chromium, bukan cuma baca kode): `.nav-cta` (Masuk/Coba gratis, atau Buka Composer/avatar kalau login) plus brand logo di baris nav yang sama ternyata **udah overflow duluan** di layar ≤414px (`document.documentElement.scrollWidth` kebukti 478px padahal viewport 375px — screenshot nunjukin tombol "Coba gratis" kepotong di kanan layar) — `.nav-links` yang di-hide di `≤860px` doang gak cukup ngasih ruang buat `.in`'s padding (`--sm-space-7`=48px tiap sisi) + gap (`--sm-space-6`=32px) + 2 tombol full-padding. Fix: breakpoint baru `@media (max-width: 480px)` di `landing.css` — padding `.in` dan gap dikecilin, tombol `.nav-cta .sm-btn` dikompakin (padding+font lebih kecil), margin-left di `.nav-hamburger` yang tadinya dobel sama flex-gap `.in` (nambah lebar gak perlu) dihapus. Diverifikasi lewat Playwright: scan `scrollWidth` di 12 lebar viewport (320–900px) — nol overflow di semua ukuran (sebelumnya overflow persis di 320–414px, pas di rentang HP paling umum).
  4. **Halaman share** (`PublicNodePanel.tsx`) — bottom-sheet-nya gak lengkap dibanding `Drawer.tsx` asli (gak render `.drawer-backdrop`/`.drawer-handle`), jadi di mobile kliatan beda: gak ada overlay gelap di belakang, gak ada grip-bar. Fix: disamain strukturnya (dibungkus fragment, tambah dua elemen yang sama) — CSS-nya udah ada dari poin "Layar kecil: sidebar jadi bottom sheet" di atas (di-scope `.page-canvas`, halaman share juga pakai class itu), jadi otomatis kepake tanpa CSS baru.
  5. **Riwayat list composer** (`.history-item`) dan **legend chip canvas** (`.canvas-legend`) — belum ada penyesuaian mobile sama sekali, agak mepet di layar sempit. Fix: gap/padding/font dikecilin dikit di breakpoint `≤560px` yang udah ada di masing-masing file.
  - **Batas verifikasi sesi ini**: landing (hamburger + fix overflow nav-cta) dan login/register diverifikasi end-to-end lewat script Playwright headless (viewport 375×812 + scan lebar 320-900px, screenshot dicek manual) — nol console error, nol overflow horizontal. Composer/canvas/share **belum** bisa diverifikasi live karena butuh session login dan gak ada akun test yang tersedia di sesi ini — perubahannya CSS-only/reuse pattern yang udah ada (bukan logic baru), tapi tetap worth di-cek manual pas ada yang login beneran.
- [x] Ganti isi `lib/api.ts` dari mock ke fetch asli — dikerjakan satu fungsi per satu, bukan sekaligus.
  - `generateMindmap()` **sudah** manggil `POST /api/mindmap/generate` beneran (`FormData`: topic/timeframe/verbosity/language/file, sesuai kontrak §6).
  - `getMindmap(id)` **sudah** manggil `GET /api/mindmap/:id` beneran. `app/canvas/[id]/page.tsx` juga dapat state error baru (`loadError` + `.canvas-loading--error`) — sebelum ini, kegagalan `getMindmap` cuma `console.error` dan macet selamanya di "Memuat canvas…"; sekarang tampil pesan "Mindmap tidak ditemukan atau gagal dimuat." + link balik ke composer.
  - `getMindmaps()` **sudah** manggil `GET /api/mindmap` beneran (dashboard "Riwayat"). Bridge sementara di `generateMindmap()` (`mindmapsDb.push`) udah dicabut, gak dibutuhin lagi. **Ini nutup bug nyata**: sebelumnya "Strategi Growth 2026" (mock seed) nongol di Riwayat semua akun tanpa terkecuali, karena mock array itu gak difilter per-user. Diverifikasi lewat Playwright: akun baru gak lagi lihat mock itu (`.history-empty` yang muncul), dan dua akun berbeda yang masing-masing generate mindmap gak saling lihat punya orang lain (`userId` filter di backend jalan bener).
  - `deleteMindmap()` **sudah** manggil `DELETE /api/mindmap/:id` beneran. **Nutup bug nyata**: sebelumnya tombol hapus cuma ngapus dari state lokal React, gak beneran manggil backend — jadi abis reload, mindmap yang "dihapus" muncul lagi (soalnya di MongoDB gak pernah kehapus). Diverifikasi lewat Playwright: hapus → hard reload → tetap gak muncul lagi (beneran hilang dari MongoDB, cascade-delete todo terkait juga jalan di backend). Sekalian nambah `deleteError` state di `app/composer/page.tsx` — dialog konfirmasi sekarang bisa gagal beneran (401/404/500), jadi kalau gagal tampilkan pesan error + dialog tetap kebuka (gak langsung nutup kayak sukses), diverifikasi lewat mock response 404 via Playwright route interception.
  - `saveMindmap()` **sudah** (lihat M9), `getTodos()`/`createTodo()`/`updateTodo()` **sudah** (lihat M8), `elaborateNode()` **sudah** (lihat M7) — semua manggil endpoint asli. Gak ada fungsi `lib/api.ts` yang masih mock lagi.
- [x] Bug background `.page-composer` setengah putih setengah krem — sama kayak bug halaman auth sebelumnya, tapi mekanismenya beda: `body` (`app/layout.tsx`) itu `display:flex; flex-direction:column`, dan `.page-composer { min-height: 100% }` (eksplisit, bukan `auto`) ngebatalin proteksi bawaan flexbox yang biasanya nyegah elemen nyusut lebih kecil dari kontennya — pas konten (form + Riwayat panjang) butuh lebih tinggi dari viewport, `.page-composer` malah dipaksa nyusut pas ke tinggi viewport, sisanya kerender pakai background putih `body`. Fix: `flex-shrink: 0` di `.page-composer`. Diverifikasi: tinggi elemen sekarang ngikutin konten asli (1231px), bukan ke-cap 700px. **Catatan buat nanti**: `.page-loading` (`loading-page.css`) punya pola `min-height:100%` yang sama persis, belum kena bug ini cuma karena kontennya selalu pendek (spinner/progress bar) — kalau kontennya pernah jadi lebih panjang dari viewport, kemungkinan besar bakal kena bug yang sama, fix-nya sama (`flex-shrink: 0`).
  - **✅ Blocker `pdf-parse` sebelumnya — partner sudah fix** (commit `fix: pdf-parse importing`): `import pdfParse from "pdf-parse"` di top-level dipindah jadi `require('pdf-parse')` di dalam blok kondisional pas ada file PDF. Diverifikasi ulang lewat Playwright: request sekarang beneran nembus ke Gemini (dibuktikan dari server log, errornya udah pindah lokasi).
  - **⚠️ Gemini API kadang balikin 503 "high demand"** buat model `gemini-3.7-flash` — ini eksternal (Google lagi overload), bukan bug siapa pun, biasanya reda sendiri. Bukan 404 (model salah nama), jadi model-nya valid.
  - **⚠️ Bug baru ketemu, backend, belum di-fix** — `GET /api/mindmap/:id` (`app/api/mindmap/[id]/route.ts`) **selalu 404 walau record-nya beneran ada**. Dibuktikan: abis generate sukses, `GET /api/mindmap` (list, tanpa filter id) nemu record-nya dengan benar; tapi `GET /api/mindmap/<id yang sama persis>` 404, konsisten walau di-hard-reload beberapa detik kemudian (nyingkirin kemungkinan replication lag). Root cause: Next.js 16 mewajibkan `params` di route handler jadi `Promise<{id: string}>`, bukan `{id: string}` langsung (`tsc --noEmit` udah nunjukin error ini dari lama, tapi baru sekarang kebukti jadi bug runtime beneran, bukan cuma cosmetic type error) — `params.id` diakses tanpa `await`, jadi `undefined` pas runtime, bikin query `findOne({ _id: undefined, userId: ... })` gak pernah match apa pun. File yang kena pola sama (dicek dari `tsc` sebelumnya): `app/api/mindmap/[id]/route.ts` (GET/PUT/DELETE — PUT/DELETE belum dites langsung tapi kemungkinan besar sama-sama kena), `app/api/mindmap/share/[shareId]/route.ts`, `app/api/todo/[id]/route.ts`. Fix-nya `{ params }: { params: { id: string } }` → `{ params }: { params: Promise<{ id: string }> }` lalu `const { id } = await params;` di awal handler — tapi itu keputusan partner, bukan sesuatu yang aku benerin sendiri.
  - **✅ Dampak ke `/canvas` (tanpa id) — udah dibenerin.** `app/canvas/page.tsx` dulu redirect statis ke `DEFAULT_MINDMAP_ID` (id mock lama, `"strategi-growth-2026"`) yang gak ada di MongoDB asli → 500 (Mongoose gagal cast string non-ObjectId ke `_id`). Sekarang jadi client component: manggil `getMindmaps()` (list udah ke-sort `createdAt: -1` dari backend, jadi index `[0]` = mindmap terbaru), redirect ke `/canvas/${mindmaps[0]._id}` kalau user punya mindmap, atau ke `/composer` kalau belum punya sama sekali / kalau fetch-nya gagal. `DEFAULT_MINDMAP_ID` (di `lib/api.ts`) dan `lib/mock/mindmap.ts` dihapus total — gak ada pemanggil lagi. Diverifikasi lewat Playwright dua skenario: akun baru tanpa mindmap → `/canvas` → `200` lalu `/composer` (bukan lagi 500); akun yang baru generate 1 mindmap → `/canvas` → `200` lalu ke `/canvas/<id mindmap itu>` persis.

---

## 10. Rencana sesi berikutnya

**✅ Selesai 2026-08-18**, kebanyakan lewat merge `development`→`frontend` (bawa 5 commit Zufar) + fix susulan sesudahnya:
- "Tandai selesai" per-node (§9 M4) — kolom `isCompleted` udah ada di backend, frontend di-wire penuh.
- **Drag Wires** (§9 M5, sisa terakhir Manual Node Edits) — connect/reconnect/delete edge manual, ternyata dikerjain Zufar barengan sama layout multi-level. Ketemu pas merge: handle node butuh diubah jadi bidirectional (`BiHandle`, id `-source`/`-target`), plus satu bug asli (bukan dari konflik) — `connectionNodeId` yang gak ada lagi di versi `@xyflow/react` yang kepasang, diganti `useConnection()`.
- **Error handling per-status** di alur generate (§9 M6) — Zufar nambahin `{"error":"REJECTED"}` signal dari Gemini buat topic berbahaya/gak relevan (balik ke form "Perbaiki Input"), plus validasi file client-side (ukuran+ekstensi) di composer. Dua-duanya nutup gap yang sama yang dicatat sesi-sesi sebelumnya.
- **Wire `userNotes`** — state-nya udah kebawa dari merge, tapi actionnya (`updateNodeNotes`) gak pernah kepanggil sampai user lapor "belum bisa simpan" — di-fix dengan `onBlur` handler, sekalian ketemu bug `prevNodeId` yang bikin `notesDraft` gak ke-seed pas node pertama diklik (lihat catatan di §9 M4).
- Parent-highlight multi-level + full-path (lihat §9 M5) — beberapa ronde fix nyusul merge yang bawa layout nested branch.

**Kandidat kerjaan berikutnya** (belum ada yang mendesak, urutan bukan prioritas):
- **M10 — sisa tombol "Install App"** (`beforeinstallprompt`) — inti PWA/offline (manifest, service worker, persist cache, disable-aksi-offline, wipe-on-logout) udah kelar DAN diverifikasi live 2026-08-18, cuma tombol install custom yang ditunda.
- **M10 — offline fallback buat hard reload/direct URL** — ketemu lewat verifikasi live: navigasi dalam-SPA (klik link) pas offline udah jalan sempurna, tapi reload/buka-tab-baru/ketik-URL-langsung pas offline masih jatuh ke error page browser native. Butuh config `next-pwa`'s `fallbacks.document` (halaman fallback khusus buat `navigate`-mode request) — di luar scope checklist M10 asli, worth dikerjain kalau mau PWA ini kepake dari kondisi "buka app dari offline dari awal" (bukan cuma "lagi buka app terus konek putus").
- **Proteksi duplikat todo yang masih rapuh** (§9 M8, ditemuin lagi lewat kerjaan Zufar) — indikator "ada tugas belum selesai" di node (`hasPendingTasks`) sama-sama masih cocokin `taskText === label` doang, karena `WireTodo` tetap belum punya field `nodeId` asli.

---

## 11. Sinkronisasi backlog vs `user_flow.md`

`user_flow.md` (root, flowchart product-level bikinan partner) gak pernah di-cross-check node-per-node ke breakdown milestone M0-M11 di §9 sejak awal project. Dicek ulang 2026-08-18, hasilnya:

**Udah tercakup di §9** (beda istilah/pola UI di beberapa tempat, tapi bukan gap fungsional):
- `A1`/`A2` (Install PWA) → M10 (belum dikerjakan, tapi udah tercatat)
- `B`/`C`/`D` (valid session, login/register, dashboard) → M11 (selesai)
- `E` (View Saved Roadmap) → M11 "Riwayat" → `/canvas/[id]` (selesai)
- `F`/`G`/`G1` (tombol create, input parameters, topic vs PDF) → M6 — catatan: `user_flow.md` gambarnya "Input Parameters Modal", implementasi sekarang halaman composer penuh (bukan modal). Beda pola UI, bukan gap fungsional; sengaja gak diubah tanpa diskusi dulu (§8, keputusan struktur komponen)
- `H` (Loading: Gemini AI Generation) → M6 `/loading` (selesai)
- `H1` cabang "Unfeasible Timeframe" → M6 `feasibilityWarning` banner (selesai)
- `H1` cabang "Yes (Valid & Feasible)" → alur normal (selesai)
- `E1`/`E2`/`E3` (network status, cached roadmap, AI actions disabled offline) → M10 (belum dikerjakan, tapi udah tercatat)
- `I` (Hybrid Canvas) → M1/M4 (selesai)
- `K` (Click node → detail + time-mark) → M5 (selesai)
- `K2` (AI Action Expand Node) → M7 (selesai: store mechanics `appendNodes`/`applyLayout`, `elaborateNode()` wired ke endpoint asli, chip konteks node di chat)
- `L`/`M`/`O` (Export PNG, Share read-only URL) → M9 (selesai)
- `P`/`Q` (Create To-Do, due dates) → M8 (selesai di sisi frontend)
- `R` (Vercel Cron email reminder) → di luar scope frontend (§2), tanggung jawab partner

**Gap fungsional nyata, baru ketemu lewat sinkronisasi ini:**
- `J` (Manual Node Edits: Rename, Delete, Drag Wires) — **gak pernah ke-breakdown ke milestone mana pun** sejak awal project. Ditambahin ke §9 M5 — Rename dan Delete udah selesai dikerjakan, tinggal Drag Wires (reconnect edge) yang masih kosong.
- `H2` (Nonsense/Dangerous input → error alert → balik ke form) dan `G2`/`G3` (PDF invalid → error alert → balik ke form) — ada di flowchart, tapi implementasi sekarang nyamain semua jenis error generate jadi satu pesan generik + tombol retry-payload-sama, gak bedain "input ditolak" (mestinya balik ke form buat ganti input) vs "gagal transient" (retry masuk akal). Dicatat di §9 M6.

Sisanya (semua node lain di flowchart) udah punya representasi yang jelas di §9 — gak ada yang kelewat lagi.
